# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repository is

A static site deployed on Netlify for **신호등옥션 (구 낙찰예보/BidCast, 2026-07-19 리브랜딩)** — an Onbid public-auction price-prediction service. There is a minimal root package.json **only for Netlify Functions dependencies** (`@netlify/blobs` — the prediction-ledger store); the site itself has no build step, no test suite, and no linter. Each page is a single self-contained HTML file with all CSS and JavaScript inlined — keep it that way when editing; do not introduce bundlers, frameworks, or external JS files. (The repo previously also hosted the K-Map Linker/K-Buddy travel apps; those files were removed 2026-07-17.)

**프로젝트 헌장**: `000_PROJECT_MANIFESTO.md`(2026-07-25) — 존재 이유·미션·데이터/AI/엔지니어링 철학·Golden Rules 10조·고정 결정 대장·ADR-000을 담은 최상위 문서. 여기 적힌 원칙(봉인 불변, 낙찰가 마스킹, 두 트랙 분리, 넓혀서 맞히기 금지 등)과 충돌하는 변경은 사람(창업자)의 헌장 개정 없이 금지. 원칙 변경 시 이 파일의 Appendix C(고정 결정 대장)에도 기록할 것.

## Files

### Serverless functions

- `netlify/functions/claude.js` — Serverless proxy for the Anthropic Messages API, used by `bidcast-bot.html`'s free-text chat. Forces the model allowlist and clamps `max_tokens`; the key lives only in the `ANTHROPIC_API_KEY` env var.
- `netlify/functions/onbid-search.js` — Serverless proxy for the 온비드(Onbid/KAMCO) auction-listing API, feeding `bidcast-list.html`. End Point is confirmed (`https://apis.data.go.kr/B010003/OnbidRlstListSrvc2`); see the BidCast real-data section below — **the exact operation path and response field names are still unconfirmed**.
- `netlify/functions/onbid-detail.js` — Serverless proxy for the "온비드 부동산 물건상세 조회 서비스" (Base URL `https://apis.data.go.kr/B010003/OnbidRlstDtlSrvc2`, operation `GET /getRlstDtlInf2`, looked up by `cltrMngNo`+`pbctCdtnNo`). `ONBID_DETAIL_API_URL` is set in Netlify and `mapDetail()` is field-mapped against a live response (2026-07-18). Returns a clean 501 when the env var is unset. `bidcast-detail.html` calls this function asynchronously for live items and gracefully falls back to the sessionStorage snapshot on any failure.
- `netlify/functions/onbid-calendar.js` — Weekday bid-count aggregator for the landing page's calendar preview: queries the 부동산 list API once per weekday (Mon–Fri, `bidPrdYmdStart`=`bidPrdYmdEnd`=day, `numOfRows=1`) and returns each day's `totalCount`. KST-aware; rolls to next week on weekends; 10-min CDN cache header.
- `netlify/functions/onbid-mvast-search.js` — Proxy for the 온비드 **동산** (movable assets: 차량/기계장비/유가증권 등) 물건목록 조회서비스, mirroring `onbid-search.js`. **Pending env var**: `ONBID_MVAST_API_URL` (Base URL from the approved data.go.kr service page; returns clean 501 until set). Operation path defaults to `/getMvastCltrList2` (inferred from the 부동산 naming pattern — override via `ONBID_MVAST_API_OP` if wrong; verify with `?debug=1`). Response mapping is tolerant/assumed-analogous to 부동산 and marks items with `assetClass:'동산'`.
- `netlify/functions/onbid-bidinfo.js` — Proxy for the "차세대 온비드 물건 입찰결과상세 조회서비스" (Base URL `https://apis.data.go.kr/B010003/OnbidCltrBidRsltDtlSrvc2`, op `GET /getCltrBidRsltDtl2` — **both confirmed from the user's approval page 2026-07-19**, 부동산·동산·자동차 커버, daily quota 1000): per-item 개찰 결과(낙찰/유찰/취소) 상세. **Pending env var** `ONBID_BIDINFO_API_URL`; response field names (esp. the `winAmt` 낙찰금액 candidates in `mapRound()`) are unconfirmed until first `?debug=1`. **더 이상 `bidcast-detail.html`의 입찰 이력 timeline에 쓰이지 않음(2026-07-20)** — 상세 페이지는 `onbid-svc?svc=cltr_dtl_bidinf`(물건상세 입찰정보, op 실호출 확정 `getCltrBidInf2`)로 전환. 이 프록시는 레지스트리 `cltr_bidrslt_dtl`과 별개로 남겨두되 현재 UI 미사용.
- `netlify/functions/onbid-bidresults.js` — Proxy for the "차세대 온비드 물건 입찰결과목록 조회서비스" (Base URL `https://apis.data.go.kr/B010003/OnbidCltrBidRsltListSrvc2`, op `/getCltrBidRsltList2` — path verified via `_health`; request params confirmed from the data.go.kr 미리보기 spec 2026-07-19: `cltrTypeCd` 0001부동산/0002자동차/0003동산 is the key mandatory param, plus `opbdDtStart/End` 개찰일, `exctStatCd` 0003=개찰완료, `pbancYmdStart/End`, `onbidPbancNm`, `orgNm`). Defaults: 부동산+개찰완료+최근 90일; `resultCode 03 NODATA` returns an empty 200, not an error. Supports `?stats=1` returning `{count, avgWinRate, median…}` over 낙찰 rows — the intended feed for upgrading the 규칙 기반 예시 predictions to real 낙찰가율 statistics. **Response field names (esp. 낙찰금액) still unconfirmed** — check `?debug=1` on first data response.
- `netlify/functions/onbid-vhcl-search.js` — Proxy for the 온비드 **차량** 물건목록 조회서비스 (부동산·동산과 별개의 세 번째 자산군). Base URL/op are pattern-inferred defaults (`OnbidVhclListSrvc2` / `/getVhclCltrList2`) — **unverified**; override via `ONBID_VHCL_API_URL`/`ONBID_VHCL_API_OP` if `_health` says the guess is wrong. Items are marked `assetClass:'자동차'`, `type:'차량'`. Merged into `bidcast-list.html` as a third parallel source.
- `netlify/functions/onbid-svc.js` — **Registry proxy for ALL ~27 approved data.go.kr services** (the user's account has 36 approvals, 2026-07-14). `?svc=_list` shows the registry, `?svc=_health` pings every service (numOfRows=2) and classifies each as `ok` / `endpoint_ok_params_needed` / `endpoint_missing` / `key_error` — pattern-inferred Base URLs/ops are corrected per-service via `ONBID_SVC_<ALIAS>_URL`/`_OP` env vars (no redeploy needed). `?svc=<alias>` proxies any registered service with raw passthrough (`items`/`totalCount`/`body`). Use this to onboard new services: run `_health`, fix `endpoint_missing` aliases with the End Point from the approval page, then build dedicated mapped proxies/UI as needed.
- `netlify/functions/rtms-svc.js` — **국토교통부 실거래가(RTMS) 승인 서비스 10종 레지스트리 프록시** (시세 추정 축의 데이터 계층, 승인 2026-07-19: 아파트 매매상세/전월세/분양권, 오피스텔·연립다세대·단독다가구 매매, 단독다가구 전월세, 토지·상업업무용·공장창고 매매). Base `https://apis.data.go.kr/1613000/RTMSDataSvc<유형>`, op `/getRTMSDataSvc<유형>` — 10종 중 9종은 승인 페이지로 확정(2026-07-19), land_trade(토지 매매)만 패턴 유추(`?svc=_health`로 판정, `RTMS_SVC_<ALIAS>_URL/_OP`로 교정). 공통 필수 파라미터 `LAWD_CD`(법정동 앞 5자리)+`DEAL_YMD`(YYYYMM); 응답이 **XML뿐**이라 프록시가 `<item>`을 JSON으로 변환해 반환(1시간 CDN 캐시). **파서 버그 수정(2026-07-26)**: 필드 정규식이 `<item>` 래퍼째 매칭해 `{item:"<원시XML>"}` 통짜로 반환하던 버그를 market-est 프로브 실호출에서 발견·수정(래퍼 제거 후 필드 매칭) — **상세 페이지 "주변 실거래" 미표시(물건 2021-09579-010)의 근본 원인이었을 가능성 높음**, 실 응답 필드명은 영문(aptNm/dealAmount/excluUseAr/buildYear/umdNm 등) 확정. 인증키는 `ONBID_SERVICE_KEY` 재사용(같은 data.go.kr 계정; 분리 필요 시 `RTMS_SERVICE_KEY`). 용도: 물건 주변 실거래 표시→현재 시세 추정→(추후) 부동산원 지수 기반 시나리오·수익 시뮬레이션.
- `netlify/functions/onbid-mvast-detail.js` — Proxy for the 온비드 동산 물건상세 조회서비스. **End Point/op 승인 페이지 확정(2026-07-20)**: Base `https://apis.data.go.kr/B010003/OnbidMvastDtlSrvc2`, op `/getMvastDtlInf2`, 필수 입력 `cltrMngNo`+`pbctCdtnNo` (override via `ONBID_MVAST_DETAIL_API_OP`). Clean 501 until `ONBID_MVAST_DETAIL_API_URL` is set. `bidcast-detail.html` routes live items with `assetClass:'동산'` here instead of `onbid-detail`. 응답 필드 매핑은 첫 실호출 `?debug=1`로 확인 후 다듬기.
- **큐레이션 "오늘의 주목 물건"(2026-07-26)**: predict-daily가 봉인과 **같은 실행**에서 마감 임박 물건(이미 가져온 `items` 재사용, 추가 온비드 호출 0)을 `lib/curation.js`의 소스 중립 엔진으로 기회 점수화 → 상위 24건을 `curated/latest`에 저장(하트비트·summary에 `curated` 카운트 추가). 봉인 로직과 **분리된 별도 패스**(봉인 코드 무변경). 저평가 여력 판단에 이번 실행 챔피언 예측(`predMap`)과 실측 셀(`cellFor` 백오프)을 근거로 사용. 랜딩 `#curated` 섹션(히어로 직후)이 `curated.js` GET으로 렌더 — **자산군별(부동산·자동차·동산) 그룹으로 묶어 각 3개만 미리보기 + "N건 더 보기"로 그 분야 전체 인라인 펼침**(24건 한꺼번에 쏟아져 압도되는 것 방지, 2026-07-26). 카드마다 주목 점수·근거 태그·D-day·AI 예상 구간, 클릭 시 상세 스냅샷 주입. "투자 추천 아님·분석 주목도" 명시, 함수 미배포/빈 데이터 시 섹션 숨김(degrade). **2단계 개인화(2026-07-26)**: `#curatedPrefs` 관심 설정 바(유형 10종 칩·지역 17개·예산 상한)가 `bidcast:profile`(localStorage, 로그인 불필요)에 저장 → `applyProfile()`이 유형·지역·예산으로 필터·주목순 재정렬, 매칭 0건이면 전체 폴백(빈 화면 금지). 리스트 페이지(`bidcast-list.html`)에 **'주목순(할인·유찰·임박)' 정렬** 추가 — `notableScore()`는 카드에 보이는 팩트만 쓰는 **경량 클라이언트 heuristic**으로, 서버 엔진(`lib/curation.js`, 봉인 예측·유사 사례 통계까지 반영)과 **별개임을 라벨로 구분**. **검색은 사용자가 찾는 도구, 큐레이션은 엔진이 가져다주는 축** — 창업자와 합의(2026-07-26).
- `netlify/functions/predict-daily.js` — **예측 장부(Prediction Ledger) 봉인** scheduled function (KST 07:00 via netlify.toml cron `0 22 * * *` UTC; manual GET works too). Collects items closing within 오늘~+2일 (부동산+동산+차량), computes an interval [lo, mid, hi] from 캠코 용도별 낙찰가율 (앵커: 감정가×rto1, 최저가×rto2; width w from `calib`, default ±18%), and seals it into Netlify Blobs store `ledger` under `pred/{cltrMngNo}_{pbctCdtnNo}`. **Sealed predictions are never overwritten** — that immutability is the trust story. Items with no statistical basis are skipped (`noBasis`), not guessed. **하트비트(2026-07-23)**: 매 실행마다(성공·예외 모두) `_run/predict-daily` Blob에 `{at, ok, sealed, error?}` 기록 → scoreboard `runs.predict`로 노출, 자가진단이 신선도로 "조용한 죽음" 감지(같은 패턴을 score-daily·collect-history에도 적용).
- **모델 승격 검증 기준 (2026-07-20 사용자 확정)**: 절차 = 동일 물건 병행 봉인·개찰 전 봉인·동일 채점·전수 기록(자동). 판정 = 미리 고정된 3단계 체크포인트만 사용(수시 판정 금지 — 우연에 속는 것 방지): ① 조기 승격: 비교 100건 시점 승률 62% 이상 ② 표준 승격: 300건 시점 승률 55% 이상 + 세그먼트 붕괴 없음(어떤 용도·가격대도 챔피언 대비 적중률 10%p 이상 하락 금지) + 같은 적중률이면 평균 구간 폭이 좁을 것 ③ 조기 탈락: 200건 시점 승률 50% 이하. 승격/탈락 결정은 사람이 하고 chronicle에 기록. 보조 지표 avgWidthPct(낙찰가 대비 평균 구간 폭 %)를 agg/aggB에 누적 — "넓게 질러서 맞히기" 방지. 참고: 셀 통계 자격 minN=20, 보정 발동 20건과는 별개의 문턱.
- **챔피언/챌린저 이중 봉인 (2026-07-19 준비 완료)**: predict-daily는 hist/_cells가 존재하면 같은 물건에 v0.5 챌린저 예측(`predb/{id}_{cdtn}`, 최저가×lr 분위수 p10/p50/p90, 백오프 L3→L0·표본 20+, **회차는 온비드 공매차수 pbctNsq 실측 우선**(목록 매핑 `round`; 없으면 유찰수+1 근사, `roundReal` 플래그로 구분))을 병행 봉인. **회차 기준 정합성(2026-07-20)**: 학습 데이터(collect-history→hist 셀)의 회차도 onbid-bidresults가 pbctNsq로 매핑하므로, 예측·학습 셀 매칭이 같은 pbctNsq 기준으로 정렬됨. score-daily가 같은 낙찰가로 둘 다 채점해 `aggB`(적중률·오차·상대전적 headToHead)에 집계, scoreboard가 `challenger`로 노출. 학습 데이터가 없으면 챌린저는 조용히 생략 — 백필이 쌓이는 순간 자동으로 경쟁 시작.
- `netlify/functions/score-daily.js` — **자동 채점** scheduled function (KST 19:30, cron `30 10 * * *` UTC). Fetches last-3-days 개찰 결과 (3 asset classes × 2 pages via onbid-bidresults), joins with sealed predictions, grades 구간 적중 (lo≤낙찰가≤hi) + 중앙값 오차(%·만원), aggregates into `agg` (overall/가격대별/용도별/일별), and **calibrates** per-usage interval width w toward 95–98% hit rate (changes appended to `log` — the "모델이 학습하는 모습" feed). `scored/*` markers prevent double-grading; 유찰(0011)/취소(0012) conditions are closed out without grading. **성능(2026-07-23)**: 개찰 결과별 scored/pred/predb Blob 조회를 순차→청크 병렬(mapLimit 40)로 선조회하도록 리팩터 — 순차 조회(최대 600건×3)가 Netlify 함수 실행시간 제한을 넘겨 502로 채점이 멈추던 문제 해결(채점 수식·집계 로직은 불변). **채점 소실 방지(2026-07-23)**: `scored/*` 마커를 루프 중이 아니라 `agg` 저장과 **같은 최종 배치**에 기록하도록 이동 — 과거엔 루프 중 마커를 쓰고 마지막 `agg` 저장 전에 함수가 죽으면 그 물건들이 다음 실행에서 `already` 스킵되어 채점이 영구 소실됐다(scoreboard `n:0`이 185일간 고정된 근본 원인). 이제 중간 실패 시 마커가 하나도 안 남아 다음 실행이 온전히 재처리한다. ⚠️ **과거 소실분은 복구 불가**(마커에 hit/errPct만 있고 win/tier/usage 없어 agg 완전 재구성 불가) — n은 배포 후 신규 개찰부터 0에서 누적 재시작. **전수 채점 보장(2026-07-27, 보강 A)**: 과거 자산군×2페이지=200건 상한이라 개찰 많은 3일 창에선 초과분이 영영 채점 안 돼 "전수 채점" 문구와 어긋났음 → 자산군별로 `numOfRows=1000`으로 **마지막 페이지까지 수집**(batch<1000이면 종료, 자산군당 최대 6페이지 안전 상한)하도록 교체. 대부분 자산군 1콜에 끝나 콜 수도 감소. **챌린저 폭 관측 계측(2026-07-27, 보강 B)**: `aggB.byLevel`(백오프 레벨 L0~L3별 n·hit·평균폭)·`aggB.roundReal`(회차 pbctNsq 실측 vs 유찰+1 근사 비율)을 채점 시 누적 → scoreboard `challenger.byLevel`/`.roundReal` 노출. 폭 335% 진단의 "어느 레벨에서 오나·회차 근사가 얼마나 많나"를 데이터로 드러냄(예측 공식 불변, 관측만). 검증: `global.__FAKE_STORE__`+fetch 목킹 fixture 14항목(1,205건 전수 수집·200 상한이면 유실될 1,000건 포착·레벨별 집계·roundReal 3/2·scoreboard 노출) PASS.
- `netlify/functions/collect-history.js` — **예측 엔진 0단계: 학습 데이터 수집기** scheduled function (KST 20:10, cron `10 11 * * *` UTC; 수동 GET 지원, `?windows=N`). 온비드 입찰결과목록(3자산군)의 과거 개찰 이력을 7일 창 단위로 과거로 백필(기본 365일, `HIST_TARGET_DAYS`)한 뒤 신규분을 매일 증분 유지. Blobs `hist/{start}_{end}/{cltrTypeCd}`에 압축 레코드(낙찰 0010+유찰 0011만: 용도/회차/감정가/최저가/낙찰가/낙찰가율/입찰자수/개찰일), 커서는 `hist/_state`, 집계는 `hist/_meta`. 실행당 최대 6창×3자산군×5페이지로 일일 쿼터(1000) 내 유지. **v0.5 다차원 통계 엔진(용도×회차×가격대 분위수)과 "유사 사례 근거 제시"의 데이터셋** — 증분 창은 겹칠 수 있어 소비 측에서 `id_cdtn` 중복 제거 필요. **증분 하우스키핑 수정(2026-07-27, 보강 C)**: 백필 완료 후 증분 경로가 매일 `hist/{start}_{end}/{type}`에 썼는데 창 키가 하루씩 이동해 6일씩 겹치는 블롭이 무한 누적되고 `meta.records`가 팽창했음 → **고정 키 `hist/_inc/{type}`로 매일 덮어쓰기**(자산군당 블롭 1개 유지)로 교체, `meta`는 증분분을 `incRecords`에 교체 기록(누적 X)하고 `records`는 과거 백필 창만 누적. hist-stats가 `hist/_inc/*`도 스캔(구형 통짜와 동일 처리, id_cdtn 중복제거로 백필과 조인). 검증: `__FAKE_STORE__`+fetch 목킹 fixture 11항목(고정 키 생성·날짜 이동 키 미생성·재실행 누적 0·meta.records 불변·incRecords 교체·hist-stats 증분 셀 반영) PASS. (⚠️ 이 함수는 `hist/{window}/{type}`에 낙찰가 포함 통짜 저장 — 구형. 과거 학습 프로젝트는 아래 `collect-backfill.js`의 **feat/win 마스킹 분리** 저장으로 이행 중, hist-stats도 그쪽으로 전환 예정.)
- `netlify/functions/collect-backfill.js` — **과거 학습 2단계: 1~6월 전수 수집 + 낙찰가 마스킹** (일반 함수 30초, 임시 스케줄 `*/5 * * * *`로 자가 구동). 2026 1~6월(`HIST_BF_START/END`) 온비드 개찰결과를 numOfRows=1000 census로 수집하되 **낙찰가를 물리 분리**: `hist/feat/{start}_{end}/{type}`=개찰 전 피처(id/cdtn/round/usage/apsl/low/st/opbd — **낙찰가·wr·lr 없음**), `hist/win/{start}_{end}/{type}`={id_cdtn:{win,wr,lr,bd}}=개찰 결과. **예측 모듈은 hist/win을 require조차 안 함**(마스킹 코드 리뷰 검증). 시간예산(~22s)만큼 수집 후 **창마다 커서(`hist/_bfstate`) 즉시 저장** → 중간에 죽어도 다음 실행이 이어받아 소실 0, 커서가 `BF_START` 도달 시 no-op. 진행 `hist/_bfmeta`{records,windows,oldest,newest,done}·하트비트 `_run/collect-backfill`은 scoreboard `backfill`/`runs.backfill`로 노출. `?status=1` 수집 없이 진행 조회, `?reset=1&confirm=1` 커서 초기화. **2026-07-23 백필 완료(112,649건, 1~6월 전수)** — netlify.toml 임시 스케줄 제거, 함수는 수동 GET 재실행용으로 유지. **2025 확장 재활성(2026-07-27 창업자 지시)**: `HIST_BF_START` 기본값 20260101→20250101로 낮춰 백필을 2025로 확장. 프로덕션 커서(2026 완료=20251231)가 그대로 2025로 이어져 **2026분 재수집 없음**(창 키가 달라 덮어쓰기도 없음). netlify.toml에 **완만 페이싱 임시 스케줄 `0 */2 * * *`**(2시간 주기≈하루 480콜, 라이브 일일 엔진 ~100콜과 충돌 없음, 2~4일 완료) 재등록 — 커서가 2025 시작 도달 시 no-op, 완료 후 스케줄 제거 예정. **부작용**: 수집 중 `hist/_bfmeta.done`이 false가 되어 sim-live/backtest가 2025 완료까지 일시 no-op(관제실은 기존 1~6월 sim 유지) → 완료 시 자동 재개 + hist-stats rebuild 1회 발화(라이브 챌린저 셀에 2025 즉시 반영). 완료 후 sim-live env(SIM_START_YM 등)를 2025로 넓혀 더 긴 학습곡선 재생 예정.
- `netlify/functions/backtest.js` — **과거 학습 4단계: walk-forward 백테스트(3단계 성장 곡선)** (일반 함수, 임시 스케줄 `*/6 * * * *`). 마스킹 수집한 1~6월로 "엔진이 학습할수록 좋아지는가"를 훔쳐보기 0으로 측정 — **확장 창**: ①1월 학습→2·3월 예측, ②1~3월 학습→4·5월 예측, ③1~5월 학습→6월 예측. 각 단계는 미래 블록을 예측하므로 자기/미래 낙찰가 누수 0. **마스킹 보장**: 예측 함수 `predictChallenger`는 `{type,usage,round,low}`만 받고 win 필드가 없음(낙찰가는 채점 시점 `hist/win`에서만 꺼냄). 챌린저(최저가 대비 낙찰가율 분위수 셀, n≥20 백오프)로 [lo,mid,hi] 산출, 단계별 **적중률·중앙값오차·구간폭·커버리지**(학습 늘수록 증가)를 `bt/{s1,s2,s3}`·`bt/summary`에 기록(scoreboard `backtest`/`runs.backtest` 노출). **라이브 원장(pred/predb/agg) 무관 — bt/* 격리.** 단계당 train/score 2페이즈로 나눠 시간예산 안에서 재개(`bt/_state`), backfill(`hist/_bfmeta.done`) 완료 전엔 no-op, 3단계 완료 후 no-op. `?status=1`/`?reset=1&confirm=1`. **2026-07-23 3단계 완료(v0.5)** — 적중률 53.5%→56.4%→62.8%, 중앙값오차 56.5%→43.9%→41.0%(학습 늘수록 개선 확인). **모델 비교(v0.6, 2026-07-23)**: v0.5 vs v0.6(두 앵커+광폭 밴드) 비교 완료 — **v0.6 기각**(적중률 상승이 밴드 1.7배 확대의 결과, 오차 악화 — "넓혀서 맞히기" 금지 위반). **모델 레지스트리 구조(v0.7, 2026-07-26)**: 모델별 predict 함수를 `MODELS` 배열에 등록하면 전 모델 병행 채점(`bt/{stage}.models` 맵, `summary.models` 라벨) — 새 후보 = 등록 한 줄 + `BT_VERSION` 범프(AIOS 모듈 확장 원칙). **v0.7 = v0.5와 동일 공식·동일 폭(p10~p90)에 저가율(최저가/감정가) 조건 셀 L4를 체인 최상위에 추가** — `discBand()`(d100≥95%/d80≥75%/d64≥55%/dlt55/na)로 신탁·깎임 깊이를 분리해 중심 정밀화, 미달 시 v0.5 체인(L3~L0) 백오프. 공통 `predictFromChain`이라 밴드 확대 경로가 구조적으로 없음. `BT_VERSION='v3-discband'`. 최상위 필드는 하위호환 위해 기준선(v0.5) 값 유지. 랩 `#btSec`는 `s.models` 맵을 일반화 렌더(구형 `s.v05/.v06` 폴백, `mkLabel`이 v05→v0.5 표기). **v0.7 기각(2026-07-26 실데이터 3단계 백테스트)**: v05 vs v07 적중률 53.5/**52.8**·56.4/**55.1**·62.8/**62.2**(세 단계 모두 v07 하락), 오차도 미세 악화 — 저가율(최저가/감정가)이 실데이터에선 이미 회차·가격대와 상관돼 L4 분리가 새 신호 없이 표본만 희석(과분할). 스모크에서 극적 개선(오차 21.2%→0.2%)은 인위적 시드 탓이었음. **live 챌린저는 원래부터 v0.5라 코드 변경 없음**(v0.7은 백테스트에만 존재). 임시 백테스트 크론(*/6) 제거(비교 완료), backtest.js는 수동 GET·차기 모델 재검증용으로 유지. discBand/keysForV07은 차기 설계 참고로 남김. 교훈: 이미 상관된 변수로 셀을 더 쪼개면 과분할로 열화 — 차기 후보는 회차·가격대와 **직교하는** 신호(지역 등) 또는 연속 보정(수축/회귀)을 검토.
- `netlify/functions/sim-live.js` — **모의 라이브 학습장(2026-07-27, 엔진 학습 관제실 2단계)** — 헌장 **Golden Rule 11(시점 정직성/No Look-Ahead)** 를 코드 구조로 강제하는 재생 엔진. 마스킹 과거를 **월 단위 확장 walk-forward**로 재생: 대상 월 M의 학습 셀은 M 시작 이전(`opbd < M-01`)으로만 빌드, 예측 함수 `predictOne(cells, feat)`의 `feat`는 개찰 전 값(`{type,usage,round,low}`)만 갖고 **win/lr 필드가 아예 없음**(미래를 훔쳐볼 경로 구조적 차단), 낙찰가는 채점 시점에만 레코드에서 꺼냄. 챌린저 v0.5 공식(최저가×lr 분위수 p10/p50/p90, L3→L0 백오프, n≥20)로 per-item 예측·채점해 **월별 학습곡선(`bt/sim/summary.months` — hitRate·cumHitRate·avgWidthPct)** 과 **per-item 기록(`bt/sim/records` — 관제실 과녁 점: lo/mid/hi/win/hit/errPct/level, 월당 60건 다운샘플)** 생성. 백테스트(3단계 집계)보다 촘촘한 곡선 + 점 하나하나를 줘 마이페이지 관제실 리플레이·과녁을 **진짜 데이터로** 채운다. **bt/sim/* 격리(라이브 pred/predb/agg 무관)**, 월당 1회 처리·커서(`bt/sim/_state`) 재개, 온비드 API 미호출(Blobs만 읽어 쿼터 무관). 기간은 env(`SIM_TRAIN_FROM`/`SIM_START_YM`/`SIM_END_YM`)로 확장 — **2025 데이터가 쌓이면 env만 바꿔 그 시점부터 재생**(코드 불변). 하트비트 `_run/sim-live`, `?status=1`/`?reset=1&confirm=1`, `hist/_bfmeta.done` 전 no-op·5개월 완료 후 no-op. 임시 스케줄 `*/15`(완료 후 제거 예정). `exports._test`로 GR11·월유틸·예측·셀 fixture 22항목 검증(예측 feat에 win 넣어도 무시=미래 차단 실증).
- `netlify/functions/agents.js` — **전문 에이전트 라우터 (expert/region/competition + rights[B]/sale_risk[D] + news[C]/macro[⑦])**. POST {agent, item 스냅샷} → 서버가 봉인 예측(pred/predb)·캠코 용도/지역 통계·hist-stats 셀을 수집해 "확인된 데이터" 블록으로 직렬화 → claude.js 프록시(Haiku, max 700토큰) 호출 → Blobs `agent/{agent}/{id}_{cdtn}` 영구 캐시(물건·조건당 1회 생성). **rights·sale_risk는 `needsPbanc:true`** → 공고 계열 체이닝(물건 → `cltr_dtl_bidinf`로 pbancMngNo·보증금·입찰조건 → `pbanc_dtl`로 공고종류(연기/취소)·취소사유·회차·직전공고)으로 공고/매각조건 팩트를 추가 수집. **news는 `needsNews:true`** → `naver-news` 프록시로 소재 시군구 부동산 뉴스 수집(NAVER 키 미설정 시 "뉴스 없음"으로 degrade). **macro는 `needsMacro:true`** → `ecos-svc` 프록시로 한국은행 기준금리·시장금리 최근값+추세(전월/전년 대비) 수집(ECOS 키 미설정 시 "거시 없음"으로 degrade). **region은 `needsRone:true`** → `rone-svc` 프록시로 부동산원 주택가격지수 최근값+3/6/12개월 변동 수집해 지역 분석에 포함(RONE_API_KEY/STATBL_ID 미설정 시 생략). 시스템 프롬프트가 "데이터에 없는 사실 생성 금지·구간으로만·보장 표현 금지·특정 입찰가/대출상품 추천 금지"를 강제, 역할별 면책 자동 부착(rights 첫 문장 "법률 자문이 아닙니다" 강제). `bidcast-detail.html` 기본정보 탭의 "AI 전문가 분석" 섹션(라이브 물건 전용, 버튼 7종)이 호출. 설계 의도·로스터·원칙은 "전문 에이전트 시스템" 메모 참조.
- `netlify/functions/audit-weekly.js` — **전문 에이전트 ⑧ "감사역"** 주간 예약 함수(일요일 21:00 KST, cron `0 12 * * 0` UTC; 수동 GET·`?force=1`). 채점 장부(agg/aggB/calib)를 읽어 **규칙 기반**(LLM 미사용 — 공개 로그 헛소리 방지)으로 ① 전체 적중률·오차·구간폭(목표 95~98% 대비) ② 용도별 최약 세그먼트 ③ 챔피언 vs 챌린저 승격/탈락 체크포인트(조기승격 100건 62%+·표준 300건 55%+·조기탈락 200건 50%-) 판정을 산출해 chronicle에 `kind:'audit'`로 append + `audit/latest` 저장. **채택(모델 변경)은 사람이 결정** — 관찰·제안만 기록. 채점 표본 `<30`이면 no-op, 주 1회 가드(6일 내 감사 있으면 스킵). 하트비트 `_run/audit-weekly`. 랩 chronicle 타임라인이 audit 항목 렌더. 로스터 8종 마지막 미구현이었던 감사역 완성(2026-07-23).
- `netlify/functions/market-est.js` — **시세 축 1단계 신뢰도 프로브(2026-07-26)**: 시군구(lawd)×유형(kind=apt|offi|rh|sh)×최근 N개월(기본 6) 실거래를 rtms-svc로 수집 → (선택) 단지명 부분일치+**전용면적 적응형 밴드**(동일 평형 ±5% 표본 5건 이상이면 그것만, 부족 시 ±20% 폴백 — `filter.areaMode` tight/wide로 표기; 은마 네이버 호가 교차 검증에서 ±20% 고정이 인접 평형 혼입으로 상단 ~3% 부풀림 확인되어 조임, 2026-07-26) 매칭 → (선택) `region=<시도>`면 rone-svc 지수 12개월 변동의 월복리율로 거래 시점→현재 보정(`adjust.applied` 플래그) → **㎡당 만원 p25/p50/p75 + 표본수 + 신뢰도 등급** 반환(`area` 주면 총액 환산도, `dong` 주면 읍면동 적응형 좁힘 — 표본 5건 이상일 때만 local, 아니면 시군구 유지 `filter.dongMode`). **2단계 확산(2026-07-26, 은마 교차 검증 합격 후)**: `bidcast-detail.html` 주변 실거래 섹션이 주거 4종(apt/offi/rh/sh)에서 market-est 시세 밴드(㎡당 p25~p75 + 신뢰도 배지 + 시점 보정 표기 + AI 예상 낙찰 구간 병기 + 네이버 부동산 아웃바운드 링크)로 업그레이드 — `insufficient`면 범위 숨기고 거래만(정직성), 비주거(토지·상가·공장)는 기존 단순 표시 + 네이버 링크 유지(개별성 커서 밴드 미제공), 시뮬레이터 `mktM2` 전달은 p50 기반으로 승격. **3단계 마진 여력(2026-07-26)**: 시세 밴드 카드에 **전용면적 입력식 마진 위젯** — 온비드가 전용면적을 안 주므로(정직성 제약, 임의 가정 금지) 사용자가 면적 입력 시에만 `추정 시세(㎡당 p25/p50/p75 × 면적) − AI 예상 낙찰 구간 − 취득비용(1.5%+300만, 시뮬레이터 동일 가정)`을 **보수~낙관 범위**로 계산(보수=낮은 시세·높은 낙찰, 낙관=높은 시세·낮은 낙찰). 보수 시나리오가 음수면 "손실 구간" 경고. 신뢰도 '참고 수준'이거나 AI 예상 구간이 없으면 마진 위젯 미표시, "수익 보장 아님" 면책 고정. **명목 마진 정직화(2026-07-26 창업자 지적)**: 유찰 18회 물건에 "+1억 마진"을 자축 표시하던 현실 괴리("이 상태 물건이면 참여 안 하는 게 맞는데 분석은 반대로 말함") 교정 — 표시를 **"명목 마진(인수 부담 차감 전)"**으로 개명하고, **유찰 3회↑ 또는 명목 마진≥시세 50%면 빨간 경고**("이만한 명목 마진에도 낙찰되지 않은 것 자체가 임차보증금 인수·명도·미납관리비·권리 하자 등 숨은 인수 부담의 시장 신호 — 명세서·등기부 확인 전 입찰 판단 근거로 쓰지 말 것" + 유찰 진단 앵커)를 마진 아래 부착, "보수 시나리오에서도 여력 존재" 자축 문구 삭제. 큰 명목 마진 = 매력이 아니라 경고로 읽게 하는 프레임. **네이버 링크 번지 정밀화(2026-07-26)**: 동 단위 쿼리 → 상세 API `lotAddr` 도착 시 `setNaverAddr()`가 번지 주소로 링크 갱신(`#naverLandLink`, 호·층 제거 정제는 지도 지오코딩과 동일 규칙, 미도착 시 동 단위 폴백). **딥링크 실검증 교정(2026-07-26 창업자 실클릭 2회)**: ① `new.land.naver.com?query=`는 딥링크 미지원 ② `m.land.naver.com/search/result/`도 풀 행정명("경기도 안성시 중리동")·소규모 단지("중리동 동광아파트") 모두 "검색결과 없음" 실사례 → **네이버 통합검색(`search.naver.com?query={q} 부동산`)으로 최종 전환**(항상 결과가 나오고 부동산 카드→네이버부동산 진입 동선). 쿼리 생성은 유지: 시도 제거("안성시 중리동 616"), 제목에서 단지명 추출 시 "읍면동+단지명" 우선(지번 lotAddr이 나중에 와도 격하 방지 가드), 비주소 제목(동산·차량)은 가드로 무시. **관련문서 열람(2026-07-26)**: 온비드 화면의 관련문서(감정평가서 PDF 등)를 우리도 연결 — 필드명 미확정이라 `deepFiles()`가 onbid-detail 원본(raw) 전체를 딥스캔(같은 객체의 http URL+문서명 짝, URL 자체가 pdf/hwp인 경우, 키워드 문서명; 이미지 제외·최대 6건·http만 링크)해 공고 카드 "관련문서" 행에 노출, 없으면 행 숨김. fixture 4종(이름+URL 짝·URL pdf·키워드명·문서 없음) 검증. **첨부파일 관용 매핑(2026-07-27 실화면 교정)**: 공고상세(pbanc_dtl) `atchFileList`가 프로덕션에서 "첨부파일 1·2·3" 비링크로 뜬 실사례(이름 키 `fileNm` 추정 미스 + URL `urlAdr` http 요구 미스) → `mapAtchFile()` 관용 매핑으로 교체(이름 = 알려진 키 → `*Nm` 키 → 확장자 있는 값 → 한글 값 → 번호 폴백; URL = 알려진 키 → http 값 스캔 → 파일 힌트(dwld/file/atch/down) 있는 상대경로만 온비드 호스트 보정 — 비파일 상대경로는 미보정으로 오링크 날조 방지), 첫 파일 객체 실필드명 콘솔 진단 로그 추가(F12로 키 확정용). **온비드 공고보기(2026-07-26)**: 온비드는 물건별 GET 딥링크 미제공(세션/POST) → 공고보기 클릭 시 `copyCltrNo()`가 물건관리번호 자동 복사 + 온비드 열기(검색창 붙여넣기 안내 — 정직한 차선책). **임대차·등기·점유 그룹 카드(2026-07-26)**: 개별 카드 나열 → `groupCard()`로 카테고리당 1장(첫 항목 + "전체 보기 (+N건)" 펼침). 이전 입찰내역 기본 표시 `HIST_SHOW=3`(진행중 포함 최근 3개). 창업자 핵심 질문("결국 얼마에 팔 수 있나")의 첫 정량 답. **정직성 장치**: 표본 3건 미만 `status:'insufficient'`(통계 미제시), '높음' 등급은 단지명 매칭+10건 이상일 때만(시군구 전체 통계는 '보통' 상한), 신고 지연 notice, 개별 거래 목록(최근 40건) 동봉 — **실물건 눈검증용 프로브**라 UI 미연결, no-store. 검증 통과 후에만 상세 페이지 시세 밴드·마진 표시·큐레이션 반영으로 확산(품질 미달 시 중단 — 헌장 Appendix C).
- `netlify/functions/doc-extract.js` — **관련문서(감정평가서 PDF 등) 텍스트 추출 + AI 정리** (2026-07-26, PDF 파싱 1단계). `?url=<온비드 파일 URL>[&summarize=1]`: 호스트 허용목록 **onbid.co.kr만**(SSRF 가드)·15MB·20초 상한·%PDF 매직 검사, `pdf-parse@1.1.1`(신규 의존성)로 최대 40쪽 텍스트 추출 — **require는 반드시 `pdf-parse/lib/pdf-parse.js` 직접**(index.js의 `!module.parent` 디버그 블록이 esbuild 번들에선 항상 참 → `./test/data/05-versions-space.pdf` ENOENT로 즉사하는 버그, 프로덕션 실클릭에서 발견·수정 2026-07-27). **스캔본 판명 1건(2026-07-27, 등촌동 하나자산신탁 물건)**: 수정 후 재클릭에서 파이프라인 전체가 정상 동작(다운로드→%PDF 매직→파싱)했고 해당 감정평가서 PDF는 **스캔본(이미지)** 으로 판정되어 "원문 열람" 정직 안내가 표시됨 — 온비드 첨부 PDF가 텍스트형이라는 보장 없음(표본 1건, 기관별 상이 가능). 스캔본 비중이 높으면 OCR(예: claude.js 프록시의 문서 vision) 도입 검토 — 창업자 결정 대기. **텍스트형 성공 실증(2026-07-27, 서현동 보람코아·코리아신탁)**: 매매계약서 11쪽·감정평가서 42쪽 추출+AI 정리 성공(임대차 승계·명도 책임·근저당 존치 등 조문 근거 포함, "기재 없음" 규율 유지) — 기관별 텍스트형/스캔본 혼재 확인. 요약 출력의 마크다운 기호 날것 노출은 `mdLite()`(전체 이스케이프 후 소제목/굵게/구분선/불릿만 변환 — 임의 HTML 주입 구조적 차단)로 교정, 성공 경로만 innerHTML(스캔·실패 경로는 textContent 유지). → 200자 미만이면 **스캔본 판정**("텍스트 추출 불가 — 원문 열람" 정직 안내, OCR 날조 금지), **HWP는 unsupported 명시**(어설픈 파싱 금지 — 공매재산명세서 hwp는 차기 과제). summarize=1이면 claude.js 프록시(Haiku 900tok)로 "문서에 기재된 사실만" 항목 정리(임대차/인수조건/면적·구조/감정근거/유의사항 — 없으면 "기재 없음" 강제, 첫 문장 법률자문 아님). 캐시 `doc/{sha1(url)}`(추출)·`docsum/{sha1(url)}`(요약) — 문서당 다운로드·생성 각 1회. 클라이언트: `bidcast-detail.html` 관련문서 행의 문서 링크마다 "📖 AI 정리" 버튼(`docAiBtn`/`docSummary`) → `#docSumBox`에 렌더, 스캔/HWP/실패는 원문 열람 안내로 degrade. **버튼 게이트 확대(2026-07-27)**: 온비드 다운로드 URL이 확장자 없는 형태가 많아 `.pdf` 요구를 제거 — 온비드 호스트 http URL 전체에 버튼(명백한 비-PDF 확장자 hwp/doc/xls/zip/이미지 만 제외 — URL과 **표시 파일명 양쪽** 검사, URL이 확장자 없는 다운로드 주소여도 이름이 `.hwp`면 버튼 생략), PDF 여부는 서버 `%PDF-` 매직이 판정하고 아니면 "원문 열람" 정직 안내. **프리뷰 실검증(2026-07-27, 물건 2026-0100-095329)**: 관용 매핑으로 파일명("매각공고260713.hwp")·링크 정상 표시, HWP 클릭 시 "PDF가 아닌 문서입니다" 정직 안내 확인 — 실 atchFileList에서 이름·URL 모두 확보됨. 검증: pdf-parse 동봉 실PDF(14쪽 82k자) fixture로 추출·추출캐시(재다운로드 0)·요약·요약캐시·스캔 경로·가드(타호스트 400/호스트위장/hwp/비PDF 매직) 통과 — 실 온비드 PDF는 egress 차단으로 배포 후 확인.
- `netlify/functions/rone-svc.js` — 한국부동산원 부동산통계정보(R-ONE) OpenAPI 프록시(지역 전문가 에이전트의 주택가격지수 데이터 소스). `SttsApiTblData.do`로 통계표의 최근값+3/6/12개월 변동(%)을 반환, `?list=검색어`로 통계표 목록(STATBL_ID 찾기), `?debug=1` 검증. 인증키 `RONE_API_KEY`(부동산원 활용신청 승인 후 발급) 미설정 시 clean 501. **통계표 ID는 `RONE_STATBL_ID` env로 지정**(+ 선택 `RONE_DTACYCLE_CD`/`RONE_ITM_ID`/`RONE_CLS_ID`) — 미설정 시 `status:'no_table'` 안내. 6시간 캐시. **다차원 처리(2026-07-21)**: R-ONE은 오래된 시점부터 반환하므로 최근 시간창(`START/END_WRTTIME`, 기본 최근 3년) 지정. 통계표가 지역·용도 등 다차원이면 시점/값 외 `*_ID`·`*_NM` 필드로 시계열을 그룹핑 — 필터(`RONE_GRP_ID` 지역/`RONE_CLS_ID` 용도/`RONE_ITM_ID` 지표) 없이 시계열이 여럿이면 `status:'multi_series'`로 후보 목록 반환(하나로 좁혀야 추세 계산). **multi_series 응답은 각 시계열의 라벨과 함께 식별 `ids`(GRP_ID/CLS_ID/ITM_ID)를 노출(2026-07-22)** — 원하는 시계열(예 전국 아파트 매매가격지수)의 ids를 그대로 env에 넣으면 단일 시계열로 좁혀져, 코드조회 없이 자가 설정 가능. **동적 지역화(2026-07-22)**: `?region=<시도>` 지정 시 지역 필터(cls/grp)를 비워 전 지역 시계열을 받아 소재 시도 라벨과 매칭되는 시계열을 반환(없으면 전국 폴백, `regionMatched` 플래그). `shortSido()`가 시도명(충청북도→충북 등) 정규화. agents.js region 에이전트가 물건 소재 시도를 넘겨 지역별 지수(부산 물건→부산 아파트 매매가격지수)를 붙임. **지역 차원은 `GRP_ID`**(예 `A_2024_00003` 용도지역별 지가지수: GRP=지역 예"전국"/"부산>동구", CLS=용도지역 500001주거, ITM=지표 100001지수) — 3개를 다 지정해야 단일 시계열. `?debug=1`에 `rawFirst`·`fieldKeys` 포함(코드조회로 지역·용도 코드 확인해 필터 지정).
- `netlify/functions/ecos-svc.js` — 한국은행 ECOS Open API 프록시(거시·금리 워처 에이전트 ⑦ 데이터 소스). **기준금리(722Y001/0101000/M) 실호출 확정(2026-07-21)** — 기본 노출은 기준금리만(`DEFAULT_SERIES`=baseRate). CD·국고채는 `817Y002` **일별 표(cycle D)** 로 조회(프록시가 D 주기 시 YYYYMMDD 범위·최근~400일·≈30일/365일 전 대비 추세로 처리) — 기본 노출에 포함(`DEFAULT_SERIES`=baseRate,cd91,tb3y). item 코드가 틀리면 INFO-200이고 macro 에이전트가 조용히 드롭(무해); `ECOS_SERIES_CD91/TB3Y="STAT,ITEM,CYCLE"`로 교정, `?series=`/`?debug=1`로 검증. 각 시리즈 최근값+전월/전년 추세(%p) 반환. 인증키 `ECOS_API_KEY`(서버 전용) 미설정 시 clean 501 → degrade. 시리즈 코드는 `ECOS_SERIES_<KEY>="STAT,ITEM,CYCLE"` env로 교정, `?series=`·`?debug=1` 지원. 6시간 CDN 캐시.
- `netlify/functions/hist-stats.js` — **v0.5 다차원 통계 조회 API**: 수집된 개찰 이력을 자산군×용도×회차×가격대 4레벨 셀로 집계(hist/_cells 캐시)해 낙찰가율 분위수(p10~p90, 최저가 대비 lr/감정가 대비 wr)와 유찰율을 반환. **마스킹 분리 포맷 소비(2026-07-23)**: 구형 통짜(`hist/{w}/{type}`, win 내장)와 **신형 마스킹 분리(`hist/feat`+`hist/win`)를 모두 읽어** id_cdtn으로 조인·중복제거해 셀을 만든다 — 집계기는 모집단 통계 산출 주체라 win을 읽지만, 산출물은 **비율 분위수뿐**이고 예측 모듈은 hist/win을 안 읽는다. 창 블롭 조회는 **청크 병렬(mapLimit)** 로 수십만 건도 30초 내 재빌드. 캐시 무효화는 `hist/_meta.updatedAt`+`hist/_bfmeta.updatedAt` 결합 태그. collect-backfill이 done 시 `?rebuild=1`을 1회 발화해 **1~6월 학습을 라이브 챌린저(predb)에 즉시 반영**. `?type=&usage=&round=&tier=`(또는 lowMan) 백오프 조회(L3→L0, minN 기본 20), `?rebuild=1` 강제 재빌드, 데이터 없으면 `status:'empty'`. **v0.5 봉인 엔진과 "유사 사례 근거" UI의 공용 데이터 레이어** — 파라미터 없이 호출하면 데이터셋 요약.
- `netlify/functions/integrations-status.js` — 외부 연동 키 설정 여부 자가진단 GET. 키를 Netlify 환경변수에 넣었을 때 실제 활성화됐는지 사용자가 바로 확인하는 용도. **시크릿 값은 절대 반환하지 않고 설정 여부(boolean)만** 노출(환경변수 존재 여부는 클라 코드로도 이미 드러나므로 위험 없음). 연동별 `stage`: `ready`(코드 준비 완료·키만 넣으면 즉시 활성 — anthropic/onbid/naverNews/kakaoMap) / `pending`(키 넣어도 코드 추가 필요 — ecos 거시금리 에이전트, rone 시세 밴드). 응답 `summary.activeNow`(현재 동작 중), `summary.readyWaiting`(키 대기 중). 캐시 no-store.
- `netlify/functions/lib/curation.js` — **큐레이션 엔진(소스 중립)**: "분석해 볼 만한 물건"의 기회 점수(주목도)를 **공통 팩트만** 읽어 산출. `fromOnbid()` 어댑터(온비드 목록 아이템 → 공통 팩트 `{source,id,cdtn,assetClass,type,usage,region,apsl,low,failCount,round,bidEnd}`) + `scoreOpportunity(f, ctx)`(저가율 깊이·유찰 누적·저평가 여력[봉인 예측 mid 또는 셀 중앙 낙찰가율]·개찰 임박을 가점, 신탁[최저가≥감정가]·감정가 미공개·통계 근거 부족은 주의 플래그). **점수는 투자 추천이 아니라 분석 주목도** — 마법 숫자로 뭉치지 않고 `reasons` 근거 태그로 분해 반환, 가점 근거 없으면 null(제외). **AIOS 확장 경계(헌장 Appendix D)**: 새 소스(법원경매/신탁/NPL)는 어댑터 함수 하나만 추가하면 점수 로직 불변 — 엔진이 소스 전용 필드명을 직접 만지지 않는다. `netlify/functions/lib/`는 서브디렉터리 비-index 파일이라 엔드포인트로 발행되지 않고 require로 번들됨.
- `netlify/functions/lib/gbtree.js` — **차세대 모델 v0.8 코어(2026-07-27 착수, 1단계)**: 순수 JS **분위수 그래디언트 부스팅 회귀 트리**. 파이썬/LightGBM(네이티브·툴체인 도입) 대신 JS로 구현해 기존 모델 레지스트리·백테스트 하니스에 그대로 얹고 Node fixture로 검증(결과 bt/* 격리). 방식 = sklearn `GradientBoostingRegressor(loss=quantile)` 골격: F0=y의 τ-분위수, 반복마다 잔차 음의 그래디언트(τ 또는 τ-1)에 회귀트리(분산감소 분할) 적합 후 잎을 잔차의 τ-분위수로 재라벨(line search), F+=lr·tree. `trainQuantileBands`가 p10/p50/p90 세 GBM을 학습해 `predictBands`가 단조 보정한 [lo,mid,hi] 반환. **셀 조회(v0.5)와 달리 피처 상호작용·연속값·이분산을 학습** — fixture(합성 조건부 분위수+이분산)에서 중앙값 MAE 1.88·보정 0.11/0.88·이분산 폭 3배 포착·상수 baseline 우위·단조성 10/10 PASS(2000행×80트리 1.9초). 시점 정직성(GR11)은 호출자(백테스트/모의 라이브)가 walk-forward로 보장(라이브러리는 넘겨받은 X,y만 사용). **다음(2단계)**: 백테스트 하니스에 v0.8 등록(피처 인코딩 + train/predict 일반화) → v0.5와 3단계 walk-forward 비교, 이겨야 채택·지면 기각.
- `netlify/functions/curated.js` — **공개 GET** — `curated/latest`(오늘의 주목 물건) 반환. predict-daily가 봉인과 같은 실행에서 산출·저장. 없으면 `status:'empty'`(랜딩 degrade), 있으면 30분 CDN 캐시.
- **큐레이션 사후 채점 3단계(2026-07-26)**: predict-daily가 주목 물건마다 `curated/pick/{id}_{cdtn}` 불변 마커(score·assetClass·apsl·reasons)를 남김(`curated/latest`는 매일 덮어써지므로 개별 보존 필요). score-daily가 개찰 결과 순회 시 이 픽을 조인해 `curatedAgg`(낙찰/유찰/취소·낙찰가율 합·AI 구간 적중·자산군별)에 집계 — **봉인 채점과 같은 `scored/*` 멱등 마커 블록에서만 tally**해 정확히 1회 반영(이중 집계 구조적 차단). scoreboard가 `curatedScore`(낙찰률·평균 낙찰가율=저평가 신호 검증·구간 적중률) 노출, 랩 `#curatedScoreSec`가 렌더(표본 없으면 숨김). **투자 수익 주장이 아니라 "선별 신호의 사후 부합도"** — 남들은 추천만 하고 끝나지만 우리는 추천의 사후 성적까지 공개(창업자 합의). 검증: fixture 통합 테스트(낙찰/유찰 집계·재실행 이중집계 0·낙찰가율·구간적중·degrade) 12항목 통과.
- `netlify/functions/scoreboard.js` — Public GET returning the ledger scoreboard: `summary{n,hitRate,avgAbsErrPct}`, byTier/byUsage/daily, calib, learningLog, recent 20 graded examples, sealDays, and **`runs`(예약 함수 하트비트, 2026-07-23)**: `{predict, score, collect}` 각각 `_run/{name}` Blob(`{at, ok, error?, 처리건수}`)을 그대로 노출 — 랜딩 자가진단이 "마지막 정상 실행 N시간 전"·"조용한 죽음" 판정에 사용. 5-min CDN cache. This feeds the landing "살아있는 성적표" and the 랩 page (실측 — 예시 없음). **지표 체계: 구간 적중률(주력, 목표 95~98%) + 중앙값 오차 %·원화 병기 + 가격대별 분해** — point-accuracy claims like "99% 정확" are deliberately NOT made (사용자와 합의된 정직성 원칙, 2026-07-19).
- `netlify/functions/map-config.js` — 카카오맵 JavaScript 앱키(`KAKAO_MAP_KEY`)를 클라이언트에 전달하는 설정 프록시. 카카오 JS 앱키는 클라이언트 공개용(도메인 화이트리스트가 실보호)이지만 다른 키와 동일하게 환경변수로만 관리하고 코드/리포에 넣지 않는다. 미설정 시 빈 값 반환 → `bidcast-detail.html`이 CSS 지도 목업을 유지(degrade-gracefully). **onbid.co.kr 등 타사 카카오 키 도용 금지**(약관 위반·도메인 불일치로 미작동). `bidcast-detail.html`의 상세 지도가 이 함수로 키를 받아 카카오맵 SDK를 동적 로드하고, 스냅샷 주소(동 단위)로 초기 표시 후 상세 API의 정확한 번지 주소(`onbid-detail`의 `lotAddr`/`roadAddr`)로 재센터한다. **지오코딩 재시도·진단(2026-07-21)**: `place()`가 주소를 원주소→호·층 제거→동/읍/면 단위 순으로 재시도(한글은 `\w` 아님 → `\b` 대신 `(?!\S)`/lookahead 사용)해 `201호` 같은 정밀 주소 실패를 흡수. 각 단계에 `[map]` 콘솔 로그(키 유무·SDK 로드·지오코딩 status) — 목업이 안 걷힐 때 F12 콘솔로 원인(도메인 미등록/키 오류/주소) 판별용.
- `netlify/functions/naver-news.js` — 네이버 뉴스 검색 API 프록시(뉴스 에이전트 C단계 데이터 소스). `?query=&display=&sort=` → 제목/요약(HTML 태그·엔티티 제거)/링크/날짜 정리 반환. 인증키 `NAVER_CLIENT_ID`/`NAVER_CLIENT_SECRET`은 서버에만(헤더 인증, 도메인 제한 없음). 미설정 시 clean 501 → 뉴스 에이전트가 "뉴스 없음"으로 degrade. 30분 CDN 캐시.
- `netlify.toml` — Points Netlify at `netlify/functions` with the esbuild bundler; declares the two ledger cron schedules. Also 302-redirects `/` to `/bidcast.html` with `force = true` (Netlify serves an existing file over a redirect without it).

### 신호등옥션 (BidCast)

A 15-page static prototype (`bidcast*.html`) for an Onbid auction winning-bid prediction service, benchmarking `yoiddang.co.kr`'s information architecture. Every page ships with **all example data clearly labeled** (예시 데이터 chips) — there is no backend and "login" is a client-side demo only. The only persistence is `localStorage` (keys `bidcast:likes`, `bidcast:likesnap`(라이브 관심물건 스냅샷), `bidcast:recent`, `bidcast:alerts`, `bidcast:mypred`(예언자 등급 봉인)), shared between the list, detail, and my-page files for the 관심물건/최근 본 물건/알림 demo features.

- `bidcast.html` — Landing page. **랜딩 다이어트·3막 재배열(2026-07-26 창업자 승인)**: "백과사전 같다" 피드백 → 11개 섹션을 7개로, 후킹→증거→행동 3막 순서로 재배열: ①히어로 ②오늘의 주목 물건 ③AI 예보 카드 ④**살아있는 성적표 통합 밴드**(`#lab` — 구 ACCURACY LAB + 메트릭 밴드 + 엔진 자가진단(SYSTEM STATUS) 3섹션을 1밴드로 흡수, 모든 JS 대상 ID(labCard/mLiveCount/esGrid 등) 보존) ⑤예언자 성장 트랙(**로그인 전용** — 비로그인 티저 삭제, 로그인 시에만 섹션 노출) ⑥기능 3스텝 콤팩트(STOP/READY/GO 라벨 유지, tool 나열 제거, 카드=허브 링크) ⑦FAQ+파이널 CTA. **시뮬레이터 섹션 삭제**(READY 카드 링크로 흡수), 봉인/채점 모달은 보존(성장 트랙 기능 의존). 검증: 문법 + 단언 44항목(순서·삭제·ID 보존·로그인 토글) PASS. 다음 단계 예정: 성적표 밴드 다크화(2단계)·히어로 라이브 티커(3단계). Hero, 3-step onboarding widget (`#onboarding`), asset-type quick search, example AI-forecast cards, mini accuracy/calendar previews, insight teaser. **엔진 자가진단 섹션 `#engine-status`(2026-07-22)**: ACCURACY LAB 다음에 위치. `loadEngineStatus()`가 `scoreboard`·`hist-stats`·`integrations-status`를 병렬 조회해 4개 카드(예측 봉인=predict-daily·자동 채점=score-daily·학습 수집=collect-history·외부 연동)의 실시간 상태를 🟢정상/🟡대기/🔴확인불가 배지로 표시 — 봉인은 sealDays 합계+lastSealAt 48h 신선도, 채점은 summary.n+updatedAt, 학습은 hist-stats status/records/cellCount, 연동은 activeNow(onbid 필수)로 판정. 모두 실측 응답 기반이며 함수 미배포/오류 시 정직하게 "확인 불가"로 degrade(가짜 초록 금지). 엔진이 스스로 매일 봉인·채점·학습 중인지 사용자가 랜딩에서 바로 확인하는 신뢰 섹션. **하트비트 기반 죽음 감지(2026-07-23)**: `runState()`가 scoreboard `runs.{predict,score,collect}` 하트비트(`_run/*`)를 읽어 **오류(ok:false)거나 30시간 넘게 실행 기록이 안 갱신되면 🔴("실행 오류"/"실행 멈춤 의심")로 오버라이드** — 간접 추론(sealDays 신선도 등)이 놓치던 "조용한 죽음(타임아웃/중단)"을 직접 잡는다. 하트비트가 아직 없으면(배포 직후) null → 기존 데이터 기반 판정에 위임. score-daily가 185일 조용히 죽어도 몰랐던 재발 방지책. **히어로 칸반 실데이터 연결(2026-07-22)**: 히어로 목업 3열(`#mCol1/2/3`)을 `loadHeroBoard()`가 실데이터로 교체 — 관심 물건(로그인 시 `bidcast:likesnap` 실제 관심, 없으면 "이번 주 임박 물건"으로 라이브 온비드 마감임박)·AI 예측 분석(라이브 온비드 마감임박, 최저가율 meter·유찰 기반 신호)·개찰 채점 완료(`scoreboard.recent` 실측 낙찰가 vs AI 예측 구간·오차%, 채점 0이면 "누적 중" 대기 카드). 온비드/봉인장부 실패 시 각 열 예시 유지·칩 "예시 데이터" degrade, 성공 시 칩 "🟢 실시간 온비드·봉인 장부". **카드 클릭→상세 연동(2026-07-24)**: 라이브 카드(`liveCard`)에 `mcard-live`+`data-id` 부여, 렌더 후 `__heroSnaps`(likes+live) 스냅샷을 `bidcast:detail`에 저장하고 `bidcast-detail.html?id=`로 이동(리스트 페이지와 동일 패턴). 개찰 채점 완료 열(scoreboard.recent)은 과거 결과라 비링크 유지. **지표·FAQ 실데이터화(2026-07-24)**: 메트릭 밴드의 "한 회차 물건 수(관측 예시)"를 `loadLiveListings`가 onbid-search `totalCount` 실측(`#mLiveCount`)으로 교체(실패 시 예시 유지), FAQ "정식 오픈" 문구를 "물건·캘린더·지도는 실시간 온비드, 적중률은 개찰 누적 후 실측 공개"로 정정. (시뮬레이터 "개찰 3자 비교" 카드는 내 모의 입찰가가 사용자 행동이라 실소스 없음 → 예시 삽화 유지.) **사용자 성장 트랙 — 예언자 등급(2026-07-20 1차 구현)**: 히어로 바로 아래 `#growth` 섹션 + 상단 nav 우측 사용자 탭. **여정 상세는 마이페이지 `#prophet`으로 일원화(2026-07-21)** — 랜딩 `#growth`는 로그인 시 **요약 카드(등급 배지+진행바+타일) + "마이페이지에서 전체 관리 →"(`bidcast-my.html#prophet`) CTA**로 축소(봉인 목록·자동채점·로드맵은 마이페이지로 이관, `renderGrowth`는 없는 요소 가드), 비로그인 시 티저. **개인 학습 트랙 — 엔진 공식 성적표(적중률/랩)와 UI·문구로 절대 분리**("내가 고른 물건·나의 연습 기록" 라벨). 데이터는 localStorage 데모: `bidcast:nick`(애칭), `bidcast:mypred`(물건별 {내 구간 lo~hi, (선택)AI 구간, 봉인시각, 개찰 낙찰가, 적중·오차}). 봉인은 모달에서 내 구간 직접 입력→개찰 후 실 낙찰가 입력으로 자기 채점(나 vs AI 비교). 등급 판정 = 채점 건수+개인 적중률(이드몬→몹소스→칼카스→테이레시아스→피티아→아폴론, 신호등 6색). **상세 페이지 봉인 연동(2026-07-20 구현)**: `bidcast-detail`의 "🔮 내 예측 봉인하기"가 물건명·감정가·최저가·AI 구간을 자동 채워 `bidcast:mypred`에 저장 → 랜딩 대시보드에 자동 반영. **관심물건 직접 봉인(2026-07-20 구현)**: `bidcast-my.html` 관심물건 카드의 "🔮 내 예측 봉인" 버튼이 같은 `bidcast:mypred` 구조로 저장(상세 페이지를 거치지 않고 바로) → 랜딩 여정에 자동 반영. **온비드 개찰 결과 자동 채점 — 1차 구현됨(2026-07-21)**: `#growth` "나의 예언자 여정"에 `live:true`+`cltrMngNo` 미채점 봉인이 있으면 "🎯 온비드 개찰 결과로 자동 채점" 버튼 노출 → `autoScoreLive()`가 `onbid-bidresults`를 3자산군(0001/0002/0003, 최근 90일 개찰완료)으로 조회해 물건 id로 조인, 낙찰(statCd 0010) 행의 `winAmt`(원)를 만원으로 환산해 자동 채점(hit/errPct/aiHit). **선행 블로커 (a) 봉인 시 물건 id 저장 — 해결됨(2026-07-21)**: 봉인 레코드에 `cltrMngNo`/`pbctCdtnNo`/`live` 필드 저장. **필드 확정 상태**: `onbid-bidresults.js`가 이미 낙찰금액을 `scfbAmt`로 매핑(코드 주석 "2026-07-19 실 응답 확정")해 `winAmt`로 정규화 — 클라이언트는 `winAmt`만 소비하므로 원시 필드명에 비의존. **다만 이 개발환경 egress 정책이 배포 도메인을 403 차단해 재검증(live `?debug=1`)은 미완**; `winAmt`가 0/미매핑이면 자동 채점이 조용히 건너뛰고 수동 입력으로 degrade(오채점 위험 0)라 안전. 유찰(0011)/취소(0012)·개찰 전·90일 밖은 채점하지 않고 수동 입력 유지.
- `bidcast-list.html` — Product search: 6 mode tabs (경매검색/예정물건/신건/인기물건/인기검색/매각결과), court/region/price/fail-count/type filters, and an **AI report modal** (기본정보/종합분석/권리분석/적정가 분석 tabs) that blurs the recommended-bid figures until the demo login completes. **온비드式 빠른검색(2026-07-26 창업자 지시)**: 온비드 랜딩 빠른검색과 동일 형식의 카드(`#qsCard`) — 좌측 자산군 세로 탭(전체/부동산/동산/차량, `assetClassOf()` 클라 필터) + 처분방식(매각 활성·**임대는 dspsMthodCd 코드 실검증 전 disabled "준비중"** — 날조 금지) + 용도 그룹(온비드 4그룹→우리 10버킷 매핑 `QS_USE_RE`, 동산 탭은 동산 유형 그룹로 전환) + **재산유형 서버 필터**(압류 0007/국유 0010/불용품 0004/기타 7코드 → `prptDivCd`, `loadRealItems`가 `QS.prptCodes` 동봉 — 기존에 UI가 없던 기능 신설) + 지역 2단(시도=서버 재조회, 시군구=불러온 목록 주소에서 추출해 클라 필터) + [물건 보기](`qsApply()`). 기존 상세 필터(기관/지역/가격대/유찰/상태/유형칩)는 `#advFilters` 접이식("조건검색 ›"로 여닫음, 기본 접힘)으로 이동, 정렬 드롭다운은 항상 노출, "지도검색 ›"은 bidcast-map 링크. `resetFilters()`가 패널 상태·서버 필터 함께 초기화. 검증: 문법 + 단언 35항목(코드 합집합=서버 전체 10코드·4그룹=10버킷 무결·자산군 판정·배선) PASS, 라이브 동작은 배포 후 확인.
- `bidcast-detail.html` — Item detail page, reached from `bidcast-list.html` card titles via `?id=N`. Resolution order: demo id (1–9) → `DETAILS`; live id (cltrMngNo) → a `sessionStorage` snapshot (`bidcast:detail`) that the list page's `stashDetail()` writes on click, rendering real basic facts + photo immediately, then **asynchronously calls `/.netlify/functions/onbid-detail`** to enrich the page with 기본정보/감정평가/임대차 등 상세 데이터 (API 미연동 시 스냅샷 유지, 칩이 연동 상태 표시); otherwise falls back to item 1. **온비드式 단일 스크롤 재구성(2026-07-26 창업자 지시)**: 기존 4탭(기본정보/종합분석/권리분석/적정가) 리포트형 구조가 **법원경매(요이땅) 템플릿**이라 공매(온비드)와 안 맞고 "탭에 정확한 기능이 없다"는 지적 → 탭 제거하고 **온비드처럼 단일 스크롤 + 두 블록 명확 구분**으로 전면 개편. 【블록 A — 온비드 물건정보(사실)】: 임대차·권리 정보(`#sec-tenancy`, onbid-detail `leasInfList` 실측 — 상위 배치)·이전 입찰내역(`#sec-hist`). **기본정보 표(`#sec-info`)는 온비드式 그리드와 중복이라 제거**(2026-07-26). **이전 입찰내역 온비드式(2026-07-26)**: 온비드 "이전 입찰내역" 메뉴 형식 — 요약 카드 3개(`#histSummary`: 이전 입찰 결과·이전 최저입찰가격·전체 유찰N/취소M회, prcnBidClgList 실측) + 회차 타임라인, 회차 많으면 최근 6개만 남기고 `applyHistCollapse()`로 접고 "더보기"(`toggleHistMore`)로 펼침. **임대차 JSON 노출 버그 수정(2026-07-26)**: leasInfList 필드 매핑이 틀려 `JSON.stringify(l)` 폴백으로 원시 JSON(`{"irstDivNm":...}`)이 화면에 노출되던 버그 → 실 필드명(irstDivNm 구분·cltrInprNm 성명·bidGrteeAmt 보증금·mthrAmt 월세·convGrteeAmt 환산보증금·cfmtnYmd 확정일자·mvinYmd 전입일) 매핑, null은 생략(노출 금지), 전부 없으면 "공매재산명세서 확인" 안내. **등기·점유·첨부 추가(2026-07-26)**: 임대차 섹션에 등기 사항(`rgstPrmrInfList`)·점유 관계(`ocpyRelList`)를 추가 — **필드명 미확정이라 `fmtObjVals()`(문자열·숫자 값만 · 로 join, null/객체/빈값 제외, HTML 이스케이프)로 안전 요약**해 원시 JSON 노출을 구조적으로 차단(leasInf 버그 재발 방지). 공고 카드에 첨부파일(`atchFileList`) 링크 추가 — `urlAdr`이 http로 시작할 때만 링크, 상대경로·미확인은 파일명만 텍스트 표기(날조 금지). 필드 라벨은 라이브 미검증(egress 차단)이나 값만 안전 표기라 무해 degrade. 【블록 B — 🚦 신호등옥션 분석(Decision 레이어)】: AI 예상 낙찰가(`#sec-ai` 봉인 예측)·유찰진단·주변 실거래·인근 매각통계·경쟁평가·AI 전문가. 맨 끝 유사물건. 두 블록은 인라인 구분선(회색 "온비드 물건정보" / 초록 "🚦 신호등옥션 분석")으로 분리 — 헌장 "Prediction은 수단, Decision이 목적"을 UI로 구현(온비드 사실 위에 우리 판단을 얹되 출처를 섞지 않음). **온비드式 물건정보 그리드(2026-07-26 추가)**: 법원경매 필드(경매구분/개시결정/감정일/배당종기/청구액) `dtbl`을 **온비드 상세와 동일한 2×4 요약 그리드**(`.ob-grid`)로 교체 — 재산유형·면적(평/㎡ 토글 `toggleAreaUnit`, ÷3.305785)·입찰방식·감정평가금액(원) / 처분방식·입찰기간(입찰시작전/입찰중/마감 상태 배지)·회차·유찰횟수·최저입찰가격(원). 감정가·최저가는 온비드처럼 **원 단위 천단위 콤마**(+억 보조 표기). 하단 **공고 정보 카드**(`.ob-notice`): 공고기관(스냅샷) + `bi.pbancMngNo`로 `pbanc_dtl` 비동기 조회해 공고유형(pbancKindNm)·공고일·참가수수료(ptctCmsn) 채움 + 온비드 공고 원문 링크. **담당부점·입찰제한정보는 API 미확인이라 창업자 판단으로 미적용**(날조 금지 — 확보 필드만). 면적은 `landAr`/`bldgAr` 실측(없으면 '-'). **제거**: 법원경매 전용 섹션(당사자/기일·등기변동/문건접수)·예시 권리분석 탭·예시 적정가 탭. **지도 옵션화(2026-07-26)**: 항상 로드하던 카카오/OSM 지도를 "📍 지도 보기" 버튼(`openLocMap`)으로 **필요할 때만 로드**(기본 접힘, `booted` 1회 가드, `__setMapAddr`는 pendingAddr에 저장 후 개방 시 반영). 온비드도 지도를 버튼으로 여는 방식과 동일. **온비드式 물건 상세 카드(2026-07-26 골격 정렬)**: 창업자 "구성이 많이 상이하다" 지적 → 큰 초록 히어로 배너·별도 정보바·중복 가격바를 제거하고 **온비드처럼 얇은 "물건 상세" 제목줄 + 사진(좌)·정보(우) 2단 카드**(`.ob-detail-card`/`.ob-detail-body` 320px+1fr grid)로 통합. 정보 열 = 칩줄(공고기관 배지·매각기일·D-day·유찰 + 우상단 [📍 지도][📄 공고보기] 액션) → 물건관리번호 → 제목 → 주소 → 온비드式 2×4 그리드 → 보증금·개찰일. 카드 하단 전체폭에 공고정보·신탁배지·지도 lazy. 감정가·최저가는 그리드에 통합돼 별도 가격바 제거(`pbAppr` 등 JS도 제거, 신탁 배지 로직만 유지). `id="rp-hero"`는 카드가 승계(사이드바 앵커 호환). 헌장 "소스 복사 금지, 우리 손으로 재현" 원칙대로 온비드 소스 미사용(온비드 페이지는 403·세션 게이트로 애초 fetch 불가 확인) — 레이아웃·정보구조만 우리 CSS로 모작. `.rp-demo-chip`은 다크 히어로용 흰 글씨였어서 라이트 카드용으로 색 조정. **허위 표현 정정**: "3개 모델 교차검증"→"챔피언·챌린저 병행 봉인"(랩과 동일 정정), 근거없는 "최근 24개월"·"낙찰 확률 70% 지점" 제거. 상세 그리드의 법원경매 필드(개시결정/감정일/배당종기)는 라이브에서 "공매 — 비해당"으로 정직 표기. `switchTab()`은 하위호환 위해 옛 탭명→섹션 스크롤로 매핑, `sbGo()`가 단일 앵커 이동. 상단 nav·사이드바·모바일탭·FAB 모두 실제 섹션 스크롤로 교체. 검증: 인라인 스크립트 문법 검사 + 구조·내용 단언 테스트(블록 순서·허위표현 제거·법원경매 섹션 제거·삭제 JS 제거 28항목) PASS. Sections(구): photo hero + key facts, AI forecast card (blur-locked until demo login, same pattern as the report modal), 기본정보 table, 입찰 이력 timeline, 권리분석 요약, stylized location mock, 유사 물건 cards (same-type first). **라이브 유사물건 실연동(2026-07-22)**: 라이브(온비드 실물) 물건이면 `loadSimilarLive()`가 **현재 물건 자산군에 맞는 목록 API**(부동산=onbid-search·동산=onbid-mvast-search·차량=onbid-vhcl-search)에서 같은 유형 우선·최저가 근접순 실물건 3건으로 유사물건을 교체(현재 물건 제외, 사진/이모지, `stashSim`으로 sessionStorage 스냅샷 주입) — 데모(1~9)는 DETAILS 유지, 실패 시 데모 유지(degrade). **교차 자산군 금지(2026-07-24 수정)**: 이전엔 항상 onbid-search(부동산)만 조회해 동산·차량 물건이 부동산을 "유사"로 표시하던 오류 + 같은 유형<3건이면 무관 유형으로 채우던 문제를 수정 — 자산군 API 분기 + 같은 자산군 내 최저가 근접순 보충으로 교정. **라이브 상세 실데이터화(2026-07-24, 2026-07-26 단일 스크롤 재구성으로 갱신)**: 온비드 공매가 제공 안 하는 법원경매 섹션(당사자/기일·등기변동/문건접수)은 **아예 제거됨**(과거엔 라이브에서 숨김 처리했음), 경쟁평가 요약의 '낙찰가 추세'를 `hist-stats`(112k 개찰 실측)의 감정가 대비 낙찰가율 중앙값·범위로 교체(셀 없으면 자산군 L0 백오프, 실패 시 예시 유지). "🟢 실측" 마킹 + 노트에 실측/참고 구분. **유찰 진단 카드(2026-07-26)**: 라이브 + 유찰 3회 이상 물건에 `#sec-failDiag` 섹션 — "왜 유찰이 반복되나"를 **규칙 기반**(LLM 미사용)으로 분해: ① 가격 요인 판정(저가율 ≤50%+유찰 5회↑면 "가격 매력 부족 아님 — 권리·점유·상태 요인 의심" 강 신호, 최저가≥감정가면 신탁 구조 신호, 감정가 0이면 판단 불가) ② 취소 반복(회차 이력 실측 `pbctStatNm` 취소 카운트) ③ 재산구분(압류/신탁 — onbid-detail `prptDivNm`) ④ 시세 밴드 존재 시 감정가 대조 안내 + **입찰 전 확인 체크리스트**(등기부 미말소 권리 → 공매재산명세서 임차인·인수조건 → 취소사유 → 현장 점유 순). 데이터가 비동기 도착하므로 `fdSet()` 패치마다 멱등 재렌더(`FD-ENGINE-START/END` 마커 — 검증 테스트가 이 블록을 추출 실행). "원인 확정 아님·확인 우선순위 제안·법률 자문 아님" 면책. **에이전트 결론 중복 방지(2026-07-26)**: 유찰 진단 카드(무료·즉시)와 AI 에이전트 rights/sale_risk/competition(온디맨드·비용 발생)의 핵심 결론이 겹치는 문제(창업자 지적: "이미 본 얘기를 또 들으면 신뢰가 깎인다") — `computeFailSignals(f)`로 신호 계산을 분리해 HTML 렌더(`renderFailDiag`)와 평문 요약(`fdSummaryText`, HTML 태그 제거)이 로직을 공유하도록 리팩터. `runAgent`가 rights/sale_risk/competition 호출 시에만 `item.failDiag`로 요약을 함께 전송 → `agents.js` `factsText`가 "[참고] 이미 화면에서 본 유찰 진단 요약(반복 금지)"로 주입하고, 세 에이전트 시스템 프롬프트에 `FD_DEDUP`("반복하지 말고 그 위에 심화하라") 공통 지시 추가. 무료 헤드라인 신호 → 유료성 온디맨드 심화라는 자연스러운 깔때기는 유지(합치지 않음), 결론 반복만 제거. **보류 과제**: 공매재산명세서(hwp 첨부) 텍스트 파싱 — 유찰 원인의 보고(임차인·인수조건 전문)이자 **신탁/NPL 물건 분석과 동성격**이라 별도 과제로(창업자 합의 2026-07-26), 현재는 "온비드 공고 원문 확인" 안내로 대체. **입찰 이력·보증금 실측(2026-07-20)**: 라이브 물건은 `onbid-svc?svc=cltr_dtl_bidinf`(물건상세 입찰정보)를 비동기 호출해 timeline을 회차별 실 최저가(`cseqBidInfClgList` 전체 회차 + `prcnBidClgList` 진행 회차 결과 병합, 미래 placeholder 날짜 2999…는 표기 생략)로 교체하고, 보증금 셀(`dDeposit`)을 `pbctTdpsCont` 실값(예 "최저입찰가격*10%")으로 실측화 — 실패 시 스냅샷/가정 유지. 이 응답의 `pbancMngNo`가 공고 계열 연결고리. **내 예측 봉인(2026-07-20)**: AI 예보 카드 아래 "🔮 내 예측 봉인하기" 버튼 → 모달이 물건명·감정가·최저가·AI 예상 구간(`window.__ai`)을 자동 채우고 사용자가 내 구간(lo~hi)만 입력 → 랜딩과 동일한 `bidcast:mypred` 구조로 저장(예언자 등급 트랙 연동). 개찰 후 채점은 랜딩 "나의 예언자 여정"에서.
- `bidcast-my.html` — 마이페이지: 관심물건 (hearts saved from list/detail via `bidcast:likes`), 추천 물건 (liked items' type/court/price를 규칙 기반 매칭하여 SUMMARY 내 유사 물건 추천 — "규칙 기반 추천 · 예시 데이터" 명시, 추천 이유 태그 표시, 매칭 점수 노출), 최근 본 물건 (recorded on detail-page views via `bidcast:recent`, capped at 12), and demo alert toggles (`bidcast:alerts`). Own `SUMMARY` dataset keyed by the shared demo ids. **라이브 관심물건 영속화(2026-07-21)**: 리스트/상세에서 라이브(온비드 실물) 물건을 좋아요하면 `bidcast:likesnap`(id→{title/min/appr/type/court/status/thumb/live/cltrMngNo/pbctCdtnNo/**bidEnd**})에 스냅샷을 저장 → 마이페이지가 데모(SUMMARY) + 라이브(likesnap) 관심물건을 함께 렌더("실시간" 칩, 상세 이동 시 `stashLive()`가 sessionStorage에 스냅샷 주입). **개찰 임박 배지(2026-07-21)**: 라이브 카드에 `bidEnd`(yyyyMMddHHmm)로 달력일 D-day 배지(`bidDday`) 표시 — D-3 이내 빨강, D-7 이내 노랑, 마감 회색; 값 없으면 생략. 라이브 관심물건 봉인은 `cltrMngNo`/`pbctCdtnNo`/`live:true`를 실어 저장 → 랜딩 자동 채점 대상이 됨. `bidcast:likes`에만 있고 SUMMARY·likesnap 어디에도 없는 id는 스킵. **관심물건 직접 봉인(2026-07-20)**: 각 관심물건 카드에 "🔮 내 예측 봉인" 버튼 → 랜딩/상세와 동일한 `mySealOverlay` 모달(내 구간 lo~hi 입력)로 `bidcast:mypred`에 저장(제목·최저가 자동 채움, appr는 SUMMARY에 없어 0). 이미 봉인된 물건은 "🔒 봉인됨 · 여정 보기"로 표시(제목 매칭). 개인 학습 트랙이므로 엔진 공식 성적표와 분리 원칙 유지. **엔진 예상 구간(AI) 자동 채움(2026-07-21)**: 라이브 물건 봉인 시 `openMySeal`이 `scoreboard?pred={cltrMngNo}_{pbctCdtnNo}`로 엔진 봉인 예측(pred lo/hi)을 조회해 `#msAiBand`로 표시하고, `doMySeal`이 이를 `aiLo/aiHi`로 함께 저장 → 개찰 채점 시 `aiHit` 계산되어 예언자 여정 "나 vs AI" 타일에 반영. 조회 실패/미봉인 물건이면 밴드 숨김·`aiLo/aiHi=null`로 degrade(수동 봉인 그대로 동작). **봉인 목록 개찰 임박 배지(2026-07-21)**: `renderProphet`의 `gwPredList`가 미채점 라이브 봉인에 `bidDday()` 배지(D-3 빨강/D-7 노랑/지난 건 "개찰 마감") 표시 — `doMySeal`이 봉인 레코드에 `bidEnd`를 실어 저장. **🔮 예언자 여정 대시보드 일원화(2026-07-21)**: 랜딩 `#growth`의 예언자 등급 여정을 마이페이지 최상단 `#prophet` 섹션으로 이식 — 등급 배지·진행바·봉인/채점/적중률/나vsAI 타일·봉인 목록(개찰 결과 입력=`openScore`)·자동채점(`autoScoreLive`)·6등급 로드맵·"+ 새 예측 봉인"(`openSeal`/`doSeal`, 수동 입력). 랜딩 대비 함수명 충돌 회피로 `renderGrowth→renderProphet`, nav 배지는 공용 배지 담당이라 `updateNavGrade` 미이식. **신규 온보딩(2026-07-21)**: 봉인 0건이면 `#prophetGuide` 3단계 시작 가이드(관심물건 담기→예측 봉인→개찰 후 채점) 노출, 첫 봉인 시 자동 숨김(`renderProphet`이 `s.sealed===0`으로 토글). **봉인 전환 넛지(2026-07-21)**: 관심물건 섹션 상단 `#likesNudge`가 관심물건 중 미봉인 개수를 안내(미봉인>0 노랑 "N개 아직 예측 전" / 전부 봉인 초록 "완료") — `renderLikes`가 `isSealed`로 계산. **상단 nav "마이" 탭 + 공용 배지 + "여정 보기" 링크가 모두 `bidcast-my.html#prophet`으로 수렴** = 개인 허브 일원화(랜딩 `#growth`는 랜딩 방문자용으로 유지). 상단 nav "마이"(→`bidcast-my.html`)로 접근. Linked from every page's footer 바로가기 column. **🎓 엔진 학습 관제실(2026-07-27, 1단계)**: 예언자 섹션 직후 `#engineRoom` — 엔진이 과거를 되감아 배우는 과정을 수학 없이 시각화(개인 예언자 트랙과 명확히 분리, "엔진의 공식 학습" 라벨). ① 학습 리플레이(다크 계기판 카드: ▶재생 시 적중률 카운터 상승 + 타임라인 진행 + 미니 과녁 점이 중앙으로 수렴, 학습곡선은 `scoreboard.backtest.stages` 실측 hitRate로 구동·없으면 "예시 데이터" 라벨로 degrade) ② 과녁 두 개(학습 초기 흩어짐 vs 학습 후 모임, 초록=구간 적중·빨강=빗나감·중앙=실낙찰가) — per-item 점 기하는 "예시 시각화" 라벨(실 per-item 재생 기록은 2단계 오프라인 학습장에서 채울 예정). CSS는 eng- 프리픽스로 스코프, 프리텐다드 팔레트 토큰 사용, reduced-motion 가드. 3·4·5(예측vs실제·모델 경주·쉬운 성적표)는 추천 순서상 다음 단계. **🗺️ 학습 로드맵 스텝퍼(2026-07-27)**: 관제실 최상단에 5단계 로드맵(①데이터 모으기 ②되짚어 학습·검증 ③학습을 눈으로[지금] ④하루씩 재생 학습장[2025 확장·모의 라이브] ⑤실전 대결·승격) — "지금 어디/다음 뭐/얼마나"를 한눈에. 실측 가능한 단계는 실데이터(①수집 records·②백테스트 학습곡선 커브·⑤챌린저 n/100 진행바), 예정 단계(④)는 시간 추정("수집 1~3일 자동+구현")을 정직 표기. 상태 뱃지 완료/진행중/예정/누적중, active 펄스는 reduced-motion 가드. `renderRoadmap(d)`가 scoreboard.backfill/backtest/challenger로 구동, degrade 시 정직 폴백. **관제실 시각화 ③④⑤ 완성(2026-07-27)**: ③예측vs실제(`renderPV` — scoreboard.recent 실측 최근 채점을 범위 막대+실낙찰가 점으로, 적중 초록/빗나감 빨강, title 등 외부데이터 esc 이스케이프) ④챔피언vs도전자 경주(`renderRace` — **적중률이 아니라 headToHead "정답에 더 가까웠던 비율"로 겨룸**: 적중률만 보면 폭 넓혀 이기는 착시라, "고정 기준 100·200·300건 도달 전 = 승격 판정 아님" 경고 부착) ⑤쉬운 성적표(`renderCard3` — summary를 "10번 중 X번 적중"·"평균 오차 X%"·"예측 폭 ±X%"로 일상어+게이지 번역). 셋 다 실측 없으면 정직한 빈 상태(EMPTY, 개찰 누적 안내)로 degrade. `renderData(d)`가 로드맵+③④⑤ 일괄 렌더. 추천 5가지 시각화 모두 구현 완료 — 다음은 실 per-item 재생 데이터를 만드는 모의 라이브 학습장(2단계, GR11 준수). **관제실 sim 실데이터 wiring(2026-07-27)**: 리플레이(①) 학습곡선은 `scoreboard.sim.months`(모의 라이브 월별 cumHitRate)를 **최우선**으로 구동(early/mid/last 3점 매핑), 없으면 백테스트 3단계, 없으면 예시로 3단 degrade. 과녁(②)은 `scoreboard.simRecords`(per-item)로 **실측 점**을 그림(`plotReal`: |errPct|→반경, hit→초록/빗나감→빨강, 초기 월 vs 마지막 월 분리 + 서브라벨 "YYYY.MM 재생·실측 N건"), 6건 미만이면 예시 유지. 로드맵 4단계(하루씩 재생)는 sim 월별 재생이 있으면 진행중/완료로 승격(3단계는 완료로), "지금 N단계" 라벨도 sim 진행 반영. 소스 칩 "실측 모의 라이브 (시점 정직성 재생)".
- `bidcast-lab.html` — Full accuracy-disclosure page. **요이땅 벤치마킹 구조 탈피 + 정직성 정정(2026-07-26)**: 기존 상단이 일반 정확도-마케팅 틀(오차분포·"평균의 함정" 2카드·자산군바) + **실제와 다른 허위 표현**("3개 AI 모델 독립분석 후 교차검증 합의")이었음 — 우리 실아키텍처(챔피언 v0.1 vs 챌린저 v0.5 병행 봉인·고정 기준 승격/기각)로 정정. 섹션 순서를 우리 고유 자산 우선으로 재배치: ①봉인 3규율(개찰 전 봉인/전수 채점/고정 기준 승격) ②백테스트 학습곡선 ③챔피언 vs 챌린저 ④**기각된 실험 기록**(v0.6·v0.7 정적 사실 카드 — "성공만 보여주지 않는다"의 실물 증거) ⑤모델 연혁 → 그다음 오차분포·자산군·캠코통계. 히어로도 "봉인한 뒤엔 고치지 않습니다"로 교체. **가짜 월별 TREND 하드코딩 제거** → `renderTrend(scoreboard.daily)` 실측(표본 없으면 "누적 중" 안내, 예시 금지). **챌린저 폭 원인 분해 `#vsLevelWrap`(2026-07-27)**: 챔피언vs챌린저 표 아래에 score-daily 보강 B의 `challenger.byLevel`(백오프 레벨 L3~L0별 표본·적중률·평균폭)을 표로 노출 + `roundReal`(회차 실측/근사 비율) 각주 — "챌린저 넓은 구간이 어느 레벨에서 오나"를 화면에 드러냄(상위 L0/L1일수록 용도 혼합으로 폭↑). byLevel 데이터 없으면 섹션 숨김(degrade). **백테스트 학습곡선 섹션 `#btSec`(2026-07-23)**: scoreboard `backtest`(bt/summary) 실데이터로 walk-forward 3단계 표(단계·학습량/예측 대상/표본/커버리지/구간 적중률/중앙값 오차/평균 구간폭)를 렌더 — 데이터 있을 때만 노출(degrade). **공식 성적표(개찰 전 봉인·전수·불변)와 UI·문구로 분리**("과거 재현·검증용 곡선" 라벨). 같은 scoreboard fetch 핸들러에서 chronicle 렌더 직전에 처리.
- `bidcast-calendar.html` — Full bidding calendar (weekday/monthly bid-count breakdown) plus a deadline-ordered 다가오는 입찰 물건 list whose rows link to `bidcast-detail.html?id=N` (own `SCHEDULE` dataset, ids match the shared demo ids). **실데이터 연동(2026-07-22)**: `onbid-calendar`(월~금 totalCount)로 요일별 건수·오늘·이번주 합계 교체, `onbid-search`를 bidEnd 기준 마감일 순 정렬해 다가오는 입찰 물건을 실물건으로 교체(라이브 상세 링크 + `stashCalLive` 스냅샷 주입). `calDataChip`이 실시간/예시 구분 표기(월별·30일은 참고 예시). 각 연동 실패 시 예시 유지(degrade).
- `bidcast-compare.html` — 비교함: side-by-side comparison of 2–3 items picked via three `<select>` slots or `?ids=1,7,5`. Own `ITEMS` dataset (shared demo ids). Highlights the unique lowest min-bid/appraisal ratio with a "할인폭 최대" chip (suppressed on ties), and blurs the AI forecast rows until the shared demo login. Entered from the list page's filter bar, the detail page's 유사물건 header (prefilled with current+similar ids), and the calendar's 바로가기 row. **라이브 관심물건 연동(2026-07-22)**: `bidcast:likesnap`의 라이브(온비드 실물) 관심물건을 `ITEMS`에 병합(cltrMngNo 키)해 실물건도 비교 대상이 됨. `?ids=` 파싱을 문자열 허용으로 변경(라이브 cltrMngNo + 데모 id 혼용). 라이브 물건 상세 링크는 realId+`stashCmpLive` 스냅샷 주입, dropdown·헤더에 "실시간" 표기·제목 이스케이프. 라이브 관심물건 없으면 예시 유지(degrade).
- `bidcast-map.html` — 실지도 페이지: 사이드 물건 목록 + 지도 핀(용도·최저가 배지), 핀↔목록 양방향 선택, 필터/검색. **이중 엔진(2026-07-21)**: `map-config`에서 카카오 JS 키를 받으면 **카카오맵**(SDK 동적 로드 + `kakao.maps.services.Geocoder`로 한국 주소 지오코딩 + CustomOverlay 핀 + InfoWindow 팝업), 키 없거나 로드 실패 시 **Leaflet+OpenStreetMap**으로 자동 폴백(Nominatim 지오코딩, 키 불필요). `USE_KAKAO` 플래그로 `initMap`/`addMarker`/`geocodeAll`/마커상호작용을 분기, 엔진 무관 헬퍼(`setMarkerActive`/`openMarkerPopup`)로 공용화. 상세 페이지와 동일하게 `KAKAO_MAP_KEY`만 넣으면 전환(카카오 콘솔에 배포 도메인 등록 필수). **카카오 렌더 경로는 개발환경 egress 차단으로 라이브 미검증** — 실배포에서 키+도메인 설정 후 확인 필요(폴백 OSM 경로는 브라우저 검증 완료). **실물건 연동(2026-07-22)**: `loadRealItems`가 onbid-search(부동산)+동산+차량을 로드해 지도 핀·목록을 실물건으로 교체(기존 하드코딩 데모 배열 대체). 지도 내부 id는 정수 유지(마커 로직 호환), 실 온비드 id(cltrMngNo)는 `realId`로 분리, 주소로 region 파생. 상세 링크는 라이브면 realId+`stashDetailLive` 스냅샷 주입. 데이터 출처/엔진 칩은 `updateMapChip`으로 일원화. 실패 시 예시 9건 유지(degrade). 지오코딩은 배포 후 카카오/OSM에서 동작.
- `bidcast-bot.html` — "예보봇" chat: preset Q&A answers are canned (instant, no API cost); free-text input calls the shared `/.netlify/functions/claude` proxy (system prompt scopes it to Onbid/공매 topics and, when `?item=N` context is present, injects that item's facts) with a graceful fallback to a canned response on any failure (function not deployed, key not configured, API error, network error) — same degrade-gracefully pattern as `onbid-search.js`. All user input and live API output render through `appendMsg()`, which HTML-escapes and converts newlines to `<br>`; only hardcoded trusted strings (presets, the context chip) go through `appendMsgHTML()` unescaped. Accepts `?item=N` (shared demo ids) to open with an item-context chip and an item-specific greeting; entered from the detail page's AI card. **예언자 등급 → 능력 티어(2026-07-20)**: `bidcast:mypred` 기반 등급(botGrade)이 예보봇 능력을 자동 언락 — **티어1(등급1~2)** 공매 절차 Q&A + **멀티턴 대화**(chatHistory 최근 8턴, 실패 시 마지막 user 턴 pop); **티어2(등급3~4)** + 라이브 물건 심층 데이터 주입(`loadDeepData`: cltr_dtl_bidinf 회차·보증금 + pbanc_dtl 공고종류를 system 프롬프트 근거로); **티어3(등급5~6)** + 전문가 에이전트 프리셋(`runBotAgent`가 agents.js의 rights/sale_risk/expert를 liveCtx 스냅샷으로 호출). 좌측에 능력 안내 카드(열림/🔒 잠김 시각화), 등급 오르면 자동 노출. `?live=1`은 스냅샷 컨텍스트 + 봉인 예측(scoreboard?pred) 조회. **AI 연동 상태 배지(2026-07-22)**: 상단 `#aiStatusChip`가 로드 시 `integrations-status`를 조회해 `activeNow`에 `anthropic` 있으면 🟢"Claude AI 실시간 답변 활성", 미설정이면 🟡"예시 응답 모드(ANTHROPIC_API_KEY 미설정)", 함수 오류면 중립 문구로 표시(가짜 초록 금지) — 사용자가 예보봇 페이지에서 바로 키 활성 여부를 확인.
- `bidcast-simulator.html` — 입찰 시뮬레이터: five sliders (감정가/입찰가율/시세/LTV/금리) drive a real-time financial calc (낙찰가, 대출액, 취득세·경비 가정 1.5%+300만, 실투자금, 월이자, 안전마진, ROI) plus a rule-based briefing (explicitly labeled 규칙 기반, not AI). Scenarios save to `localStorage` (`bidcast:sims`, capped 10) with restore/delete. Prefill via `?appr=&ratio=&id=&title=` from the detail page's 모의 입찰해보기 CTA. Also includes 모의 경매 대전: rolls 3 random competitor bids (65–108% of 감정가, uniform) against the current 나의 입찰가 (최고가 wins), explicitly labeled as a random simulation rather than an AI prediction; win/loss record persists to `localStorage` (`bidcast:battle`). The engine was ported from a user-supplied HTML after review — its yoiddang CSS hotlinks, fake AI-agent branding, and fabricated stats were removed in the port. **사용 가이드 초/중/고급(2026-07-24)**: `<main>` 상단에 접이식 `#guidePanel`(신호등 3탭 🟢초급/🟡중급/🔴고급) — 각 탭이 "이 단계를 끝내면~" 목표 + 실제 슬라이더로 하는 **실습 미션 1개** + 핵심 용어. 초급=감정가·입찰가율(낙찰가·안전마진), 중급=LTV·금리·실투자금·ROI, 고급=시나리오 비교·모의 경매 대전·주변 실거래·개찰 채점(나vsAI). 안내 전용(기능 무변경), 규칙 기반·투자 자문 아님 라벨 유지. 인라인 style/script로 자체 완결(탭 전환+접기).
- `bidcast-insight.html` — 부동산소식 + 전문가컬럼 merged into one page (kind tabs → category chips → article-detail modal). **부동산소식 실연동(2026-07-22)**: `loadNews(cat)`가 `naver-news` 프록시로 카테고리별 검색어(NEWS_QUERY: 전체/부동산소식/경매동향/시장분석)로 네이버 실시간 뉴스를 조회해 `renderArticles`가 실뉴스 카드(제목·요약·날짜, 클릭 시 원문 새 탭)로 교체, 카테고리별 캐시(realNews). 외부 데이터라 esc()로 이스케이프, 링크는 http(s)만 새 탭. NAVER_CLIENT_ID/SECRET 미설정 시 501 → 예시 유지·칩 그대로(degrade). **전문가 컬럼은 실 소스가 없어 예시 유지**(생성 시 날조라 금지) — 칩도 "부동산소식은 실시간·컬럼은 예시"로 명시. **중복 보도 제거(2026-07-26)**: 캠코 등 공공기관 보도자료가 여러 매체에 문구만 바꿔 재게재되어 노출 슬롯이 같은 내용으로 도배되는 문제(창업자 지적) — `naver-news.js` display 상한 10→30(중복 제거 후에도 채울 여유분 확보), `loadNews`가 24건을 받아 `dedupeNews()`로 걸러 9건만 표시. 판정 근거는 `newsNumberSig()` — 제목+요약에서 3자리 이상 숫자+단위(억원/억/만원/건) 토큰을 뽑아 겹치면 재게재로 간주(헤드라인이 달라도 본문 핵심 수치가 같으면 중복, 다른 회차·다른 수치는 보존 — 단어 유사도보다 안정적). 실사례(캠코 압류재산 7541억원/1796건 공매 기사 9건 중 6건 중복) 회귀 테스트로 9→4건 축소·무관 기사 보존·다른 회차 보존 검증 완료.
- `bidcast-category.html` — Region/type/기관(구 court) 3-axis SEO template; hub view with no query params, leaf view via `?axis=region|type|court&value=...`(세 번째 축 URL 키는 하위호환 위해 `court` 유지, 라벨·의미는 **기관**). **leaf 대표물건 실연동(2026-07-22)**: `loadRepItems`가 region 축은 onbid-search 지역 필터, type 축은 클라이언트 필터로 실물건 대표 카드+전체 건수 교체(라이브 상세 링크 + `stashCatLive` 스냅샷). **세 번째 축 법원→기관(공고기관) 실연동(2026-07-22)**: 온비드 공매엔 법원 관할 개념이 없고 공고기관(orgNm)이 주체라, 가공의 "법원"을 실제 공고기관 축으로 교체. 허브 기관 블록은 `loadHubOrgs`가 onbid-search 표본(100건)을 orgNm으로 집계해 상위 기관 실명·표본 건수로 노출(예시 법원명 대체). 기관 리프는 예시 데이터셋 밖의 실 공고기관 값도 허용하고, 대표 물건·물건유형 분포·표본 물건수를 표본에서 실시간 집계하며 스위처 기관 셀렉트를 `refreshCourtOptions`로 실제 기관 목록으로 갱신. 기관 건수는 **표본(sample) 기준**임을 칩·타일에 명시(정확 totalCount는 서버 orgNm 필터 부재로 불가). **리프 카테고리 스위처(2026-07-22)**: 리프 상단에 지역·유형·기관 3개 축 `<select>`를 모두 노출(`buildSwitcher`) — 같은 축 값 전환뿐 아니라 축 자체 전환(지역→유형→기관)도 한자리에서, 현재 축은 "· 현재" 강조. 상태 타일·칩 실시간 표기(분포 통계·허브 지역 건수 외는 표본/참고 예시). 실패 시 예시 유지(degrade).
- `bidcast-partner.html` — CPA partner-program landing with a revenue slider simulator and an accordion FAQ.
- `bidcast-pricing.html` — 요금제: 무료/프로(19,900원/월)/프리미엄(49,900원/월) 3단 카드, 월간·연간 토글 (연간 2개월 무료), 14항목 기능 비교표, FAQ 6문. "예시 요금제" disclosure. 모든 CTA는 공용 auth modal로 연결.
- `bidcast-support.html` — 8-tab support hub (공지/이벤트/FAQ/1:1문의/자유게시판/가이드/언론기사/제휴문의); tabs are addressable via URL hash (`bidcast-support.html#faq`).

## 전문 에이전트 시스템 (2026-07-20 사용자 승인 — 설계 의도 메모)

**왜 만들었나**: 사용자의 창업 취지는 "사용자가 스스로 공매 전문가로 성장하는 도구". 이를 위해 ① 여러 전문 분야의 분석을 사용자에게 근거로 제시하고 ② 그 분석을 수치 피처로 만들어 예측 정밀도를 높이려는 목적. 사용자가 직접 요청한 4분야(법률/부동산/뉴스·정보/지역) + Claude가 예측 기여도 기준으로 추가 제안해 승인받은 4종(경쟁 강도/매각조건 리스크/거시 금리/감사역) = 총 8종 로스터.

**절대 원칙 (수정 시에도 유지)**: 에이전트의 정성 분석은 **봉인된 예측을 직접 수정하지 않는다**. 반영 경로는 두 가지뿐 — (a) 사용자에게 근거로 표시, (b) 수치 피처화 → 챌린저 모델 → 봉인·채점 검증 통과 후 반영. 팩트가 없으면 생성하지 않고, 모든 출력에 역할별 면책 부착(특히 권리분석: 법률 자문 아님 — 변호사법 리스크로 명칭도 "권리분석 도우미" 사용). **출력 형식 원칙**: 현재 `claude.js`는 순수 프록시라 에이전트 출력은 **자유서술(prose)**. **구조화(JSON 스키마/tool-use) 전환은 피처용 에이전트(rights/sale_risk 등 수치 피처화 대상)에 한정하고, 표시용 에이전트(expert/region/news/macro)는 prose 유지.**

**아키텍처**: `netlify/functions/agents.js` 라우터 1개에 에이전트 레지스트리(역할별 시스템 프롬프트 + 컨텍스트 빌더). 클라이언트가 물건 스냅샷을 POST → 서버가 봉인 예측(pred/predb)·캠코 통계·hist-stats 셀 등 실데이터를 추가 수집 → claude.js 프록시(Haiku) 호출 → Blobs `agent/{agent}/{id}_{cdtn}` 캐시(물건·조건당 1회 생성, 비용 통제).

**전문가 자산군 3분기(2026-07-26 창업자 승인)**: expert가 `item.assetClass`로 페르소나 분기 — 부동산(기존)/**동산**/**차량**(`systemFor`/`nameFor`, maxTokens 1000). 공통: 첫 단락 "물건 1차 정리"(확인된 필드만) 강제. **동산은 품목 카테고리 감지**(`detectMvCategory` — 명품시계/명품잡화/귀금속/골프회원권/콘도회원권/골동품·미술품/전자기기/기계장비/유가증권/일반 폴백)로 **시세 확인 채널**(명품시계=구구스·바이버·크로노24 등 — 창업자 지정)과 **품목 확인 포인트**(보증서·공람 실견·현상태 인도·가품 환불 조항)를 팩트 블록에 주입. **원칙: 시세 "숫자"는 절대 생성 금지** — "채널에서 확인한 시세 − (최저가+부대비용)" 계산 프레임만 제공, 특정 입찰가·진입 회차 추천 금지, 업자 vs 개인 경쟁 구도는 일반 경향으로만(파텍필립 예시 대화 기반 설계, 시세 숫자·타이밍 추천만 제외하고 골격 재현). 차량=엔카·KB차차차·헤이딜러 채널 + 압류·저당·과태료 승계 체크. 클라이언트(`bidcast-detail.html`)는 동산·차량에서 부동산 전용 4버튼(지역/권리/뉴스/거시) 숨기고 전문가 라벨 교체(💎 동산 전문가/🚗 차량 전문가). 검증: `exports._test`로 라우팅·감지·팩트 직렬화 25항목 fixture PASS(생성 품질은 배포 후 실물건 확인). **로스터·단계**: A(구현됨 2026-07-20) ①부동산 전문가(종합분석) ②지역 전문가(지역 브리핑) ⑤경쟁 강도(유찰·관심순위·유찰율 근거) / **B(구현됨 2026-07-20) ③권리분석 도우미** — 공고 계열 연동으로 공고종류·재공고·입찰조건 확인 포인트 강화 / **C(뉴스 구현됨 2026-07-20) ④뉴스·정보(news)** — naver-news 프록시로 소재 시군구 부동산 뉴스 수집·브리핑(NAVER_CLIENT_ID/SECRET 키만 대기, 미설정 시 degrade) / **⑦거시 금리 워처(macro) 구현됨(2026-07-21)** — ecos-svc 프록시로 한국은행 기준금리·시장금리 최근값+추세 수집, 금리 국면·자금조달 시사점 브리핑(ECOS_API_KEY만 대기, 미설정 시 degrade, 특정 대출상품·입찰가 추천 금지) / **D(구현됨 2026-07-20) ⑥매각조건 리스크(sale_risk)** — 공고종류(연기/취소)·취소사유·직전공고·보증금·대금납부·입찰제한을 팩트로 매각조건 리스크 안내(권리관계 판단 아님, 공고상세는 hwp 첨부라 텍스트 전문 대신 구조화 필드 활용) / **E ⑧감사역(macro와 별개, 구현됨 2026-07-23)** — `audit-weekly.js` 주간 예약, 규칙 기반으로 채점 성적 감사(약한 세그먼트·챔피언vs챌린저 승격 체크포인트)해 chronicle에 개선 제안 기록, 채택은 사람이 결정.

## 사용자 성장 트랙 — 예언자 등급 시스템 (2026-07-20 승인 · **랜딩 1차 구현됨**)

**왜 만드나 (창업 취지 직결)**: 서비스의 목적은 "사용자가 스스로 공매 입찰 전문가로 성장". 이를 위해 사용자가 **관심 있는 물건만 골라 스스로 예측·시뮬레이션**하고, 개찰 후 채점받아 실력을 키우는 개인 학습 경험을 제공한다.

**절대 원칙 — 두 트랙 분리 (2026-07-20 사용자와 합의)**: 아래 두 가지는 성격이 다르며 **UI에서 절대 섞지 않는다**.
- **① 엔진 공식 성적표**(봉인 장부): 조건 내 **전수·불변**, 선정 기준 **고정**. "우리 모델은 이만큼 정확하다"의 공적 근거. 기준을 바꾸면 적중률의 의미가 퇴색되므로 **절대 변경 금지**. 라벨: "전수·봉인·불변 — 우리 모델의 공식 성적".
- **② 사용자 개인 트랙**(학습 기록): 사용자가 **관심 물건만** 자유롭게 골라 자기 예측·시뮬레이션. "나는 이만큼 성장했다"의 사적 기록. 여기선 사용자가 기준을 자유롭게 정해도 정직성 훼손 아님(모델 성능 주장이 아니라 개인 연습이므로). 라벨: "내가 고른 물건 · 나의 연습 기록". **비유: ①=기상청 공식 예보 적중률(고정·전수), ②=개인이 스스로 맞춰보는 연습(자유)** — 개인 연습이 공식 적중률을 훼손하지 않음.

**6단계 예언자 등급 (랜딩 신호등 3단계 × 2)**: 랜딩의 STOP(1단계)·READY(2단계)·GO(3단계)를 각 2로 세분해 총 6등급. 그리스 예측신으로 명명하고 신호등 색을 입힌다(초보 빨강 → 마스터 초록). **이름·색·수치는 초안 — 구현 시 확정**:

| 등급 | 랜딩단계 | 그리스 예측신(초안) | 색(사용자 지정) | 제안 hex |
|---|---|---|---|---|
| 1 | STOP-1 | 이드몬(Idmon) — 갓 예언을 시작한 아르고호의 젊은 예언자 | Red | `--red #E5484D` |
| 2 | STOP-2 | 몹소스(Mopsus) — 이드몬을 이은 예언자 | light Red | `#F2777A` |
| 3 | READY-1 | 칼카스(Calchas) — 그리스군 종군 예언자 | Deep Orange | `--logo #EB763C` |
| 4 | READY-2 | 테이레시아스(Tiresias) — 테베의 눈먼 대예언자 | Light Orange | `#F5A623` |
| 5 | GO-1 | 피티아(Pythia) — 델포이 아폴론 신탁의 무녀 | light green | `#5CC98D` |
| 6 | GO-2 | 아폴론(Apollon) — 예언의 신, 최고 등급 | Deep Green | `--green #00A86B` |

**승급 기준 골격 (개인 트랙 — 구체 수치는 데이터 축적 후 확정)**: 개인이 관심 물건에 자기 예측을 **개찰 전 봉인** → 개찰 후 **자기 채점**(개인판 "나 vs AI"). 등급은 개인 예측 채점 건수 + 개인 적중률 조합으로 상승. 예(초안): ①가입·첫 관심물건 → ②첫 예측 봉인 → ③채점 10건 → ④채점 30건+적중 40% → ⑤채점 60건+적중 55% → ⑥채점 100건+적중 65%. **개인 트랙 등급 기준도 한 번 정하면 고정**(자꾸 바꾸면 등급 의미 퇴색 — ①과 같은 논리).

**이미 있는 씨앗(재활용·확장 대상)**: `bidcast-my.html` "나의 성장"(`bidcast:myscore`), `bidcast-simulator.html` 시뮬레이터, 기존 "나 vs AI 채점". 이들을 "사용자 개인 트랙"으로 일관되게 묶고 등급 시스템을 얹는다.

**구현 상태(2026-07-20)**: 랜딩(`bidcast.html`)에 1차 구현 완료 — (a) `bidcast:mypred` 데이터 모델 + 등급 판정 ✅ (b) 상단 사용자 탭(애칭+등급 배지)·6등급 로드맵 UI ✅ (c) 히어로 아래 `#growth` 대시보드 ✅ (d) 봉인/채점 모달(내 구간 직접 입력 + 선택적 AI 구간 → 나 vs AI) ✅. **구현 완료(2026-07-21)**: 관심물건 봉인 시 엔진 예상 구간(AI) 자동 연동(`openMySeal`→`scoreboard?pred`→`aiLo/aiHi`, 상세 페이지는 `window.__ai` 기존 연동), 온비드 개찰 결과 자동 채점(`autoScoreLive`), 다른 페이지로 사용자 탭 전파(공용 배지). **잔여(미구현)**: 상세 페이지 온디맨드(비봉인) 물건의 실시간 AI 예측 생성(현재는 predict-daily가 봉인한 물건만 AI 구간 조회 가능).

## 보류 중인 작업 (사용자 지시로 연기 — 2026-07-19)

- **회원등록(정식 인증) — 의도적 미구현, 정식 오픈 직전 착수(2026-07-26 창업자 합의)**: 현재 `bidcast-*.html`의 auth modal은 순수 프론트 데모(진짜 OAuth·이메일·세션 없음)이고 개인 트랙은 전부 localStorage로 완결 — 로드맵 1단계(신뢰 루프 증명)는 로그인 없이 다 보여줄 수 있으므로 지금은 데모 로그인 유지가 맞다. 진짜 회원 시스템(이메일 발송·비밀번호 해싱·세션/토큰·OAuth·개인정보 DB)은 정적+서버리스+Blobs 경계(ADR-000)를 넘어 별도 인증 서비스(후보: **Netlify Identity 또는 Supabase Auth**)가 필요 → 검증 안 된 단계에서 고정비·운영부담·개인정보보호법 책임을 지지 않는다. **착수 트리거(하나라도 실사용 요구로 확인될 때)**: ① 결제(요금제 과금 — 가장 강력) ② 실 알림(관심물건 개찰 임박·새 매칭 물건을 이메일/푸시 발송 — 지금 알림은 데모 토글) ③ 기기 간 동기화 요구. 착수 시 기존 데모 auth modal UI를 실제 백엔드에 연결하는 교체 작업이 된다(UI는 이미 있음).
- **주변 실거래/시세 추정 기능 마무리** — **보류 해제(2026-07-26 창업자 결정)**: "결국 얼마에 팔 수 있느냐"가 사용자 최종 질문 → 마진(=시세−낙찰가−비용) 축의 전제로 시세 축 착수. **1단계 신뢰도 프로브 `market-est.js` 구현됨**(아래 함수 목록 참조) — 실물건 눈검증 통과 후에만 상세 페이지 확산·마진 표시·큐레이션 반영으로 확대(품질 미달 시 중단). 이하 기존 보류 메모: 현재 상태: `rtms-svc.js` 10종 전부 `_health` ok 확인, `bidcast-detail.html`에 "주변 실거래" 섹션 배포됨(조건 미충족 시 자동 숨김이라 무해). 남은 일: ① **군 단위 법정동코드 확장 — 완료(2026-07-22)**: `RTMS_LAWD`를 254 시군구(군 82곳 포함, 강원·충북·충남·전북·전남·경북·경남 군 전체)로 확장. 틀린 코드는 빈 결과→섹션 숨김으로 degrade라 무해. ② 섹션 실물건 표시 검증(사용자 물건 2021-09579-010에서 미표시 — 용도 매핑 보강 PR #27 이후 재검증 안 됨, egress 차단으로 라이브 검증 미완), ③ 시뮬레이터 "예상 시세"에 실거래 ㎡당 중앙값 참고치 주입 — **1차 구현(2026-07-22)**: 상세 RTMS 섹션이 ㎡당 중앙값 계산 시 시뮬레이터 링크에 `mktM2`(㎡당)+`mktRef`(문구)를 부착, 시뮬레이터가 시세 슬라이더 아래 참고 노트로 표시(총 시세는 온비드가 전용면적 미제공이라 사용자가 면적 곱해 조정). 총 시세 자동 계산은 면적 데이터 부재로 불가, ④ 부동산원 가격지수 연동(활용신청 필요) → 6개월~3년 보수/기준/낙관 시나리오 밴드, ⑤ 시세 추정 봉인·채점 루프 편입.
- **AI 리포트 모달 실물화 — 완료(2026-07-22)**: `bidcast-list.html`의 리포트 모달 4탭이 실물건(공매조건번호 보유)에 전부 실데이터 연결됨 — 기본정보(실시간 온비드 데이터), 종합분석(`rpAgent('expert')`→agents.js), 권리분석(`rpAgent('rights')` 도우미, 법률자문 아님), 적정가 분석(`renderPricePane`→`scoreboard?pred` 봉인 예측, blur-lock + 미봉인/실패 시 degrade). 데모(1~9)는 예시 유지. 별도 감정평가 탭은 없음(감정 데이터는 상세 페이지 담당).
- **랩 페이지 실측화 — 배선 완료(2026-07-22), 데이터 대기**: `bidcast-lab.html`이 scoreboard를 조회해 오차 범위별 예측 비율(`errDist`)·자산군별 정확도(`byAsset`)를 실측치로 교체(표본 0이면 기존 "목표치 예시" 유지 — degrade). 이를 위해 `score-daily.js`에 오차 누적 버킷(`errBuckets` le5/le10/le15/le20, 중앙값 오차 절대값 기준)과 자산군별 집계(`byAsset` — pred.assetClass 부동산/차량/동산 등)를 추가하고 `scoreboard.js`가 `errDist`(누적 %)·`byAsset`을 노출. 모델 연혁·챔피언vs챌린저는 기존대로 실데이터. 실제 수치 노출은 score-daily 채점이 쌓이는 즉시 자동. (egress 차단으로 라이브 검증은 미완 — 목킹 스모크로 실측 교체·degrade 확인.)
- **목록 회차(공매조건) 정렬 — dedupe 문제 아님으로 판명(2026-07-20)** — 물건 2022-0100-002855의 감정가(2.56억)<최저가(3.73억)는 dedupe 탓이 아니라 **실데이터가 그러함**: 상세 API(`rlst_dtl`) 직접 호출에서도 `apslEvlAmt=256000000`, `lowstBidPrcIndctCont=373248000`으로 목록과 동일. 신탁공매 등에서 최저가가 감정가보다 높게 설정되는 정상 케이스로 보임. → dedupe 교정 불필요. 다만 UI에서 "최저가>감정가"가 오해를 줄 수 있으니 안내 배지 표시 — **완료(2026-07-22)**: `bidcast-detail.html` 가격 바 아래 `#trustSaleBadge`(min>appr&&appr>0일 때 노출)로 "최저입찰가가 감정가보다 높음 — 신탁/특수 매각, 할인 아님" 안내.

## API 연동 대기 기능 (기능 ← 필요 API 매핑, 2026-07-20 정리)

각 기능이 어떤 API/키를 기다리는지의 역방향 색인. 연동되는 즉시 해당 기능을 진행할 것.

**① 온비드 endpoint_missing — 승인 페이지 End Point 스크린샷만 있으면 즉시 등록** (레지스트리 기본값 교정 또는 `ONBID_SVC_<ALIAS>_URL` 설정, 재배포 불필요):
- **주력 서비스는 전부 확정됨(2026-07-20)** — 부동산·동산·차량 목록/상세, 공고상세, 공고상세 물건정보, 물건상세 입찰정보, 공고상세 입찰정보, 통계, 코드/주소. 아래 ②-a 참조.
- 유가증권 상세(scrt_dtl), 정부재산 4종(gov_*), 수탁(trust_nbiz), 국유입찰대상(ntnl_bidtrgt) ← 물건 커버리지 확장 (낮은 우선순위, 여전히 endpoint_missing). **현재 서비스가 다루지 않는 자산군** — 커버리지 확장 시 각 승인 페이지 End Point 스크린샷으로 교정. 순위물건 조회수(rank_inqcnt)는 data.go.kr에서 서비스 중지(레지스트리 disabled).

**②-a 레지스트리 값 (2026-07-20 승인 페이지 스크린샷). 실호출 검증은 아래 ②-b 참조 — "스크린샷 확정" ≠ "작동 확정"이었음:**
- **공고상세 물건정보**(pbanc_dtl_cltr) ✅: `OnbidPbancCltrDtlSrvc2` / `/getPbancCltrInf2`, 필수 pbancMngNo. 실호출 NODATA=엔드포인트 정확. 응답: 공고에 속한 물건 목록(유찰횟수/회차·공매차수/감정평가금액·최저입찰가격 등).
- **공고상세**(pbanc_dtl) ✅✅: `OnbidPbancDtlnfSrvc2`(철자 주의 — "Dtlnf", 중간 I 없음) / `/getPbancDtlInf2`, 필수 pbancMngNo. 실호출 완전 성공. 응답: **pbancKindNm(공고종류 예 "연기공고" — 매각조건 리스크 신호)**·pbancRtrcnRsnCont(공고취소사유)·pbancNsqNm(공고회차)·pbancOrgNm(공고기관)·ptctCmsn(참가수수료)·rltnPbancMngNo(직전 공고)·atchFileList(공고문 원문 hwp 첨부 urlAdr — 텍스트 전문 아님).
- **물건상세 입찰정보**(cltr_dtl_bidinf) ✅✅: `OnbidCltrBidDtlSrvc2` / `/getCltrBidInf2`(**op 대문자 I** — 스크린샷 소문자 판독 오류였음), 필수 cltrMngNo+pbctCdtnNo. 실호출 완전 성공. 응답 핵심: **pbancMngNo(공고관리번호 — 공고번호 연결고리!)**, pbctNsq(공매차수)/usbdNft(유찰횟수), **pbctTdpsCont(보증금="최저입찰가격*10%")**, **cseqBidInfClgList(회차별 최저입찰가 8단계)**, prcnBidClgList(직전 유찰이력). → 예측 엔진 실제 회차·보증금 실측 소스.
- **공고상세 입찰정보**(pbanc_dtl_bidinf) ❓: `OnbidPbancBidDtlSrvc2` / `/getPbancBidInf2`, 필수 pbancMngNo. **실호출 미검증**.
- **코드/주소**(code_addr) ❓: `OnbidCodeSrvc` / `/getOnbidUsgCodeInfo`·`/getOnbidDtlAddrInfo`. **실호출 미검증**.
- **동산 물건상세**(mvast_dtl) ❓: `OnbidMvastDtlSrvc2` / `/getMvastDtlInf2`, 필수 cltrMngNo+pbctCdtnNo. **실호출 미검증**.
- **차량 물건상세**(vhcl_dtl) ❓: `OnbidCarDtlSrvc2` / `/getCarDtlInf2`, 필수 cltrMngNo+pbctCdtnNo. **실호출 미검증**. 전용 매핑 프록시 미작성.
- **부동산 물건상세**(rlst_dtl) ✅✅: `OnbidRlstDtlSrvc2` / `/getRlstDtlInf2` — 실호출 완전 성공, onbid-detail.js 매핑 완비. 필드: apslEvlAmt/lowstBidPrcIndctCont/ltnoPnu(PNU)/apslEvlClgList(감정이력)/cltrEtcCont(시설)/locVntyPscdCont(위치) 등.

**②-b 실호출 검증 결과(2026-07-20, 물건 2022-0100-002855)** — onbid-svc에 `?_op=`/`?_base=` 오버라이드 추가(endpoint 후보 탐색용):
- 재확정 필요: pbanc_dtl(Base URL), cltr_dtl_bidinf(op — getCltrBidInf2 대문자 I 후보). 미검증: pbanc_dtl_bidinf/code_addr/mvast_dtl/vhcl_dtl.
- **공고번호 연결고리 — 해결됨(2026-07-20)**: cltr_dtl_bidinf 응답에 `pbancMngNo`(예 `202201-02628-03`)가 포함됨 → 물건번호(cltrMngNo+pbctCdtnNo)로 cltr_dtl_bidinf 호출 → 응답에서 pbancMngNo 획득 → pbanc_dtl/pbanc_dtl_cltr 호출 순서로 공고 계열을 물건에 연결 가능.
- **온비드 공고 계열 전부 실호출 확정(2026-07-20)**: pbanc_dtl(공고상세)/pbanc_dtl_cltr(공고 물건정보)/pbanc_dtl_bidinf(공고 입찰정보)/cltr_dtl_bidinf(물건상세 입찰정보) 모두 확정. 미검증 잔여: code_addr/mvast_dtl/vhcl_dtl(주력 아님).
- ✅ 완료(2026-07-20): 상세 페이지 입찰이력·보증금 실측화(cltr_dtl_bidinf), 예측 엔진 회차 pbctNsq 실측 연결(목록 매핑 `round` → predict-daily 챌린저).
- 남은 매핑 대상: 차량 상세 전용 프록시+필드 매핑, 동산 상세 필드 매핑, 공고 계열(pbanc_dtl Base URL 재확정 후 권리분석·매각조건 텍스트 피처).

**③ 외부 키 — 6종 전부 활성 확인(2026-07-24 `integrations-status`: `activeNow:[anthropic,onbid,naverNews,kakaoMap,ecos,rone]`, `readyWaiting:[]`)**:
- **ECOS**(거시·금리 워처 ⑦), **네이버 뉴스**(뉴스 에이전트 ④), **KAKAO_MAP_KEY**(실지도), **ANTHROPIC**, **ONBID** — 키 설정 완료·활성. ECOS CD·국고채 시리즈 코드, 카카오 배포 도메인 등록은 라이브 `?debug=1`/F12로 최종 검증만 남음(키 자체는 있음).
- **RONE**(부동산원 지수) — **완전 활성(2026-07-24)**: `RONE_STATBL_ID=A_2024_00178`("(월) 지역별 매매지수_아파트") 실호출 확정, `status:ok` 단일 시계열(전국 아파트 매매지수 최신 130.32/2026-05, 변동 3m +0.6%·6m +2.1%·12m +4.8%). CLS=전국·ITM=지수로 자동 해석, 지역 에이전트가 `?region=<시도>`로 소재지별 지수 동적 선택. (추후 확장: 6개월~3년 시나리오 밴드 = 시세 축 — 여전히 보류)
- (보류·상용) 중고차 시세 DB(엔카 등) ← 차량 시세 근거. 공공 대안 검토 선행

**④ 확보 완료, 기능 보류 중**: RTMS 실거래가 10종 (전부 _health ok) ← 시세 축 재개 시 "보류 중인 작업" 참조

## Development commands

There is nothing to build or test. To work locally:

```bash
# Static pages: open the HTML file directly, or serve the repo root
python3 -m http.server 8000

# To exercise the serverless functions locally (requires Netlify CLI):
ANTHROPIC_API_KEY=sk-... ONBID_SERVICE_KEY=... ONBID_API_URL=https://apis.data.go.kr/... netlify dev
```

Deployment happens through Netlify on push (the initial commit was created via Netlify). All secrets/endpoints are configured in the Netlify dashboard, never in code: `ANTHROPIC_API_KEY`, `ONBID_SERVICE_KEY`, `ONBID_API_URL`, `ONBID_DETAIL_API_URL` (set), and `ONBID_MVAST_API_URL`, `ONBID_MVAST_DETAIL_API_URL` (+ optional `ONBID_MVAST_API_OP`/`ONBID_MVAST_DETAIL_API_OP` operation-path overrides) for the 동산 services (pending). 카카오맵 실지도용 `KAKAO_MAP_KEY`(카카오 JavaScript 앱키; 미설정 시 상세 페이지가 지도 목업 유지) — 클라이언트 공개용 키지만 다른 키와 동일하게 대시보드에만 두고 코드/리포에 넣지 않으며, 카카오 콘솔 플랫폼 Web에 배포 도메인 등록 필요. 네이버 뉴스 에이전트용 `NAVER_CLIENT_ID`/`NAVER_CLIENT_SECRET`(개발자센터 "검색" API 앱; 미설정 시 뉴스 에이전트가 "뉴스 없음"으로 degrade) — 서버 헤더 인증이라 도메인 제한 없음, 코드/리포 금지.

## Architecture

### Claude API calls

`netlify/functions/claude.js` exists so the Anthropic key stays server-side: it accepts a POST with a Messages API payload, forces the model to `claude-haiku-4-5-20251001` (allowlist) and clamps `max_tokens` to ≤1500, then forwards to `api.anthropic.com` with CORS headers. Any new or modified AI feature must call this proxy, never the Anthropic API directly from client code.

### bidcast-list.html search engine

`ITEMS` (`let`, not `const`) starts as a hardcoded example array (region/court/type/price/fail-count/tags) and is replaced in place by `loadRealItems()` on page load if `/.netlify/functions/onbid-search` returns usable data; on any failure (function not deployed, key not configured, API error, network error) it silently keeps the example array and updates the `#dataSourceChip` label to say so — the page must never break or show empty state just because live data isn't wired up yet. `applyFilters()` combines the active mode tab, active type chips, free-text query, and the five `<select>` filters, then calls `renderList()`. The AI report modal (`openReport(id)`) renders four tabs from `grade()`/`renderReport()`; the "advice" figures live inside `.rp-blur-wrap.locked`, which CSS-blurs until `userLoggedIn` flips true via the shared demo auth flow.

### onbid-search.js — real-data integration status

`netlify/functions/onbid-search.js` proxies data.go.kr's `한국자산관리공사_차세대 온비드 부동산 물건목록 조회서비스` v1.0.0 (Base URL `https://apis.data.go.kr/B010003/OnbidRlstListSrvc2`, operation `GET /getRlstCltrList2`), mirroring `claude.js`'s pattern (env-var key, CORS, clear 500 if unconfigured).

**Fully confirmed 2026-07-16** against the service's own Swagger spec (the user supplied the full data.go.kr page HTML, which has the swagger.json inlined) — request params, response field names, and code tables are all verified, not guessed:
- Required params: `serviceKey`, `pageNo`, `numOfRows`, `resultType` (must be `json`), `prptDivCd` (재산유형코드, comma-separated; `onbid-search.js` defaults to all 10 codes via `ALL_PRPT_DIV_CD` unless the caller passes one), `pvctTrgtYn` (수의계약가능여부 Y/N; defaults to `N`).
- Response item fields wired into `mapOnbidItem()`: `cltrMngNo`/`pbctCdtnNo` (identifiers), `onbidCltrNm` (title), `lctnSdnm`/`lctnSggnm`/`lctnEmdNm` (address, joined — there's no single full-address field), `orgNm` (announcing agency), `cltrUsgSclsCtgrNm` (usage sub-category, used for type bucketing), `apslEvlAmt` (appraisal amount), `lowstBidPrcIndctCont` (min bid price — a **string** that may read "비공개" instead of a number, so it's regex-parsed with a fallback to the appraisal amount), `usbdNft` (fail count), `pbctStatCd` (bid status code — `0010` = 낙찰, everything else maps to 진행), `thnlImgUrlAdr` (real thumbnail photo URL — passed through as `photo` after an http(s)-prefix check; `bidcast-list.html` renders it over the emoji `thumb` and falls back to the emoji via `onerror` when the image is missing or fails to load), `cltrBidBgngDt`/`cltrBidEndDt` (bid period, yyyyMMddHHmm strings — passed through as `bidStart`/`bidEnd`), **`landSqms`/`bldSqms` (면적 ㎡ — 토지·건물, `landAr`/`bldgAr`로 매핑, 2026-07-26 `fieldKeys` 진단으로 확인)**. Request also passes through `bidPrdYmdStart`/`bidPrdYmdEnd` (yyyyMMdd bid-period search window, Swagger-confirmed) — used by `onbid-calendar.js`. **면적 실측 발견(2026-07-26)**: 목록 API가 면적(`landSqms` 토지·`bldSqms` 건물)을 제공한다는 것을 `?debug=1`의 `fieldKeys` 진단으로 확인(예 물건 landSqms=16.501·bldSqms=71.57) — 상세 API(`onbid-detail`)엔 면적이 없으므로 목록의 이 두 필드가 유일한 면적 소스. 상세 페이지 `buildLiveDetail`이 `landArea`/`bldArea`를 이 값으로 표시(없으면 "공고 원문 확인"), 마진 위젯 `#mgnArea`가 `bldgAr`로 자동 채움(전용면적이 다르면 수정 안내) → **신탁 포함 주거용 전 물건에서 PDF 파싱 없이 면적 확보**(감정평가서 PDF는 임차인·인수조건 등 비면적 정보에만 필요).
- **Verified against live traffic 2026-07-17:** this API does **NOT** use data.go.kr's usual `{ response: { header, body } }` envelope — `header`/`body` sit at the **top level**. The parser resolves `raw.response ?? raw` to accept both. Also required-in-practice params `dspsMthodCd` (0001=매각) and `bidDivCd` (0001=인터넷) are sent by default (the Swagger marks them optional but the 활용가이드 request table marks them 필수). The same item recurs as one row per 공매조건(회차); the proxy dedupes by `cltrMngNo`, keeping the first row. A `?debug=1` query flag makes the function include the effective params and the raw upstream body's first 800 chars in its response — used for live debugging since the Netlify free plan hides preview function logs.
- Optional passthrough params wired: `lctnSdnm` (region — the function maps the front-end's short names 서울/경기/... to the full 시도명 the API expects) and `onbidCltrNm` (keyword). `bidcast-list.html` re-queries the API when the region select or search box changes while in live mode (450ms debounce on typing, stale responses discarded via a sequence counter); a re-query failure keeps the current list, and only the *initial* load falls back to demo data. The front-end's `type` filter (아파트/토지/상가/차량/...) is **not** forwarded upstream — there's no confirmed usage-category code table, so type bucketing happens entirely in `mapOnbidItem()`'s `normalizeType()` after the fact, and `bidcast-list.html`'s `applyFilters()` does the actual bucket filtering client-side. **희소 유형 보완(2026-07-26)**: 첫 페이지 조각에 희소 유형이 아예 안 걸리는 문제(실측: 재고 내 아파트 2,422건인데 목록 표시 0건)를 두 겹으로 완화 — ① 목록 로드 `numOfRows` 60→200 ② **단일 유형 칩 선택 시 서버 키워드 재조회**(`liveTypeRequery`, `TYPE_KEYWORD` 맵: 아파트/오피스텔/다세대/단독/상가/사무/공장/임야 — 제목 `onbidCltrNm` 검색이 서버 측 유형 필터의 최선). 사용자가 검색어 입력 중(qTerm)이면 그 검색 우선, 딥링크 `?type=`도 첫 로드 후 1회 재조회, `_typeKw`로 검색창 경로와 상태 동기화. **유형 세분화(2026-07-24)**: `normalizeType(usg, title)`가 예전 3버킷(아파트/토지/상가/기타)에서 **UI 칩과 동일한 10버킷**(아파트/오피스텔/단독주택/연립다세대/토지/상가/사무실/공장창고/농지임야/기타부동산)으로 확장 — 버킷명을 칩 `data-type`과 일치시켜 필터가 그대로 걸림. **용도 코드가 애매하면 물건명(onbidCltrNm)으로 폴백** 판별(예: 용도 미상 "연립주택"이 기타부동산 대신 연립다세대로 분류). 순서상 연립/오피스텔이 '주택' 키워드보다 먼저, 밭/논("전"/"답")은 용도 코드 정확 일치에 한해 농지임야(제목 "전농동" 오탐 방지). 세분 버킷명이 predict-daily의 `STAT_BUCKET` 키와도 일치해 캠코 용도별 통계 매칭도 개선됨.

**동산 (movable assets) integration — 목록 LIVE (2026-07-19):** `onbid-mvast-search.js` proxies the 동산 물건목록 service — Base URL `https://apis.data.go.kr/B010003/OnbidMvastListSrvc2`, operation `/getMvastCltrList2` **verified against a live response** (the pattern-inferred guess was correct; ~3,200 items). Response fields are the same family as 부동산 (`cltrMngNo`/`onbidCltrNm`/`cltrUsgSclsCtgrNm`/`apslEvlAmt`/`cltrBidBgngDt|EndDt`/`thnlImgUrlAdr`…), plus 동산-specific ones (`prptDivNm` e.g. 불용품, `bidDivNm` 전자입찰). **Some 동산 items legitimately have `apslEvlAmt`=0** (e.g. school 불용품) — `bidcast-list.html`'s `fmtWon()` renders 0 as "미공개". Env vars must be identical across ALL deploy contexts (a wrong Deploy-Previews-only value cost a debugging round — production had List, previews had Dtl). `bidcast-list.html`'s `loadRealItems()` fetches 부동산+동산 in parallel via `Promise.allSettled` and merges — a 동산 failure never breaks the 부동산 list; the `#dataSourceChip` says which sources are live. The 동산 **상세** proxy (`onbid-mvast-detail.js`, op guess `/getMvastDtlInf2`) is wired but its operation path is **not yet verified** — check `?debug=1` on first use from a 동산 detail page.

**Detail API live:** the "온비드 부동산 물건상세 조회 서비스" (`/getRlstDtlInf2`) is proxied via `onbid-detail.js`, field-mapped against a live response (2026-07-18), and called asynchronously from `bidcast-detail.html` for live items (동산 items route to `onbid-mvast-detail`). `bidcast-list.html`'s AI report modal still uses generated/example detail data, not live per-item detail.

**Landing page (`bidcast.html`) live wiring:** the 3 AI-forecast cards load real items from `onbid-search` (photo-first, one unit per building via a `buildingKey` heuristic, `numOfRows=60` to survive 회차 dedupe) with the prediction block explicitly labeled 규칙 기반 예시; **범위 산출 교정(2026-07-24)**: 기존 [감정가×rto1, 최저가×rto2]의 min~max 방식은 두 앵커가 벌어질 때(신건 등) 범위가 비현실적으로 넓어짐(사용자 제보 예 1.2억~4.3억) → 감정가×rto1로 중심을 잡고 **최저가 바닥 처리 + ±15% 밴드**로 교체(낙찰가≥최저가 제약 반영, 세 제보 케이스 재현 검증); the weekly calendar preview loads real per-day counts from `onbid-calendar`. Both keep the hardcoded example markup as fallback and update their 예시 chips to 실시간 labels only on success.

**Also worth noting:** the demo `ITEMS` array's `court` field (e.g. "서울중앙지방법원") implies court-run judicial auctions (법원경매), but Onbid is KAMCO-run public auctions (공매) — real items populate `court` from `orgNm`/`rqstOrgNm` (공고기관명, e.g. 코리아신탁주식회사), which won't be district-court names. `bidcast-list.html` handles this by rebuilding the 기관 filter dropdown (`rebuildCourtOptions()`) from whatever dataset is active, so the options always match the data.

### Shared auth modal pattern

Every `bidcast-*.html` page repeats the same self-contained auth modal (`#authOverlay`, `openAuth()/closeAuth()/snsFlow()/finishAuth()/logoutDemo()`) rather than importing a shared component, per the single-file convention. It is a **pure front-end demo**: any SNS button or email code instantly "logs in" and swaps the nav-right buttons for a `.nav-user` badge — there is no real OAuth, email delivery, or session storage. Keep new pages consistent with this pattern rather than inventing a new auth UI.

**공용 회원 배지 — 전 페이지 동일 위치 (2026-07-20 구현)**: 로그인 회원 표시(애칭 + 예언자 등급)를 **모든 페이지 nav 우측 같은 위치**에 렌더하기 위해, `bidcast.html`을 제외한 14개 페이지의 `</body>` 직전에 공용 배지 스크립트(`<!-- 공용 회원 배지 (예언자 등급) -->` 마커, IIFE `window.__bcBadge` 가드)를 삽입했다. 이 스크립트는 (a) 페이지 로드 시 `bidcast:nick`이 있으면 nav 요소(`#navRight`/`#rpNavRight`/`.nav-right`/`.nav-actions` 폴백)에 등급 색 배지를 렌더, (b) 페이지 자체 `finishAuth`를 래핑해 로그인 시 `bidcast:nick`을 저장하고 배지로 교체, (c) `logoutDemo`를 래핑해 로그아웃 시 애칭만 제거(개인 기록 `bidcast:mypred`는 보존). 등급 판정·색·이름은 랜딩과 **동일 로직**(`bidcast:mypred` 기반 6등급, GC/GN 상수). **배지 클릭 시 `bidcast-my.html#prophet`으로 이동(2026-07-21 — 개인 허브 일원화, 이전 `bidcast.html#growth`)**. `bidcast.html`은 자체 `updateNavGrade()`가 있어 제외. 개인 학습 트랙이므로 엔진 공식 성적표와 UI 분리 원칙 유지.

## Conventions

- **시점 기준 정직성 / No Look-Ahead (2026-07-27 창업자 지시 · 헌장 Golden Rule 11)**: 어떤 시점 T 기준으로 예측할 때 **T 이후에 발생·공개된 어떤 데이터도 예측 입력에 쓰지 않는다** — 낙찰가·캠코 통계·부동산원 지수·금리·뉴스·시세 등 모든 미래 정보 포함. 학습·백테스트·모의 라이브는 T 이전 데이터만으로 예측하고 T 이후 값은 채점(정답 확인)에만 사용. 강제 수단: 낙찰가 마스킹(feat/win 물리 분리)·walk-forward·purge/embargo·**as-of 외부데이터**(통계/지수/금리는 예측 시점 기준값). 오프라인 학습장·모의 라이브·2025 백필 설계·구현 시 이 원칙을 구조로 강제할 것(위반 = 미래를 훔쳐본 것 = 성적 무의미). 상세는 `000_PROJECT_MANIFESTO.md` 15절 Golden Rule 11 + Appendix C(2026-07-27).
- **이중 검증 원칙 (2026-07-26 창업자 지시)**: 모든 구현 작업은 완료 선언 전에 **서로 다른 각도의 검증 2회**를 거치고 결과를 명시적으로 보고한다. ① 1차 = 개발 중 스모크/단위 테스트. ② 2차 = **1차와 독립적인 방법**의 추가 검증 — 우선순위: (a) 실호출/실데이터(`?debug=1`, 프로덕션 GET — egress 차단 시 사용자에게 확인 URL 제공) > (b) **실제 응답 구조 그대로 재현한 fixture** 회귀 테스트(목킹을 내 가정대로 만들지 말 것 — rtms-svc 파서 버그가 이렇게 새어나감: 스모크는 통과했지만 목킹이 가정대로 생겨서 실응답 구조 불일치를 못 잡음) > (c) 정적 검사(문법·디프 재독). ③ 검증 결과는 PR 본문과 사용자 보고에 "검증 결과" 항목으로 남기고, **라이브 미검증 항목은 정직하게 '미검증'으로 표기**한다(스크린샷 확정 ≠ 작동 확정 원칙의 확장).

- Comments and UI copy are in Korean; preserve that.
- Section banners in the big HTML files use box-drawing/`═` comment dividers — follow that style when adding sections.
- Styling is plain CSS with custom properties defined in `:root` per file; reuse the existing variables rather than hardcoding colors.
- **BidCast palette** ("night pedestrian-signal"): light neutral background (`--bg:#F6FAF7`), green accent (`--green:#00A86B`) for go/primary actions, red (`--red:#E5484D`) reserved for warnings — always paired with an icon/label, never color alone. All 15 `bidcast*.html` pages share this token set and a standardized 8-item top nav (물건검색/지도/카테고리/적중률/캘린더/인사이트/예보봇/**마이**); `마이`(→`bidcast-my.html`)는 개인 허브 진입 탭(2026-07-21 추가, 예보봇 다음, `bidcast-my.html`에선 `class="on"`). `bidcast-detail.html`은 리포트형 nav라 예외. keep new BidCast pages' nav and footer in sync with the others rather than drifting.
- **디자인 시스템 v2 (2026-07-19, 클로드 디자인 병합)**: `bidcast.html`(랜딩)은 클로드 디자인 시안을 그대로 채택한 새 레이아웃(히어로 칸반 목업, STOP/READY/GO, 파란 파이널 CTA 밴드). 공통 요소는 **타이포 Nunito(제목)/Poppins+Noto Sans KR(본문, Google Fonts)** 와 **가로형 3도트 신호등 로고**(#EB763C/#FFC000/#27A857) — 나머지 14페이지에는 `</head>` 직전의 "디자인 시스템 v2 공통 오버라이드" `<style>` 블록으로 전파되어 있음(마크업 무변경, `.logo-mark` CSS 오버라이드). 새 페이지를 만들면 이 블록을 같이 넣을 것. 디자인 원본은 claude.ai/design 프로젝트 "웹서비스 랜딩 페이지 디자인".
- Every BidCast data point that isn't wired to a real API must carry a visible "예시 데이터" disclosure — this is a hard requirement from the legal-risk notes in the original product memo, not just a style choice.
- **파비콘(2026-07-21)**: 루트 `favicon.svg`(가로 3도트 신호등 로고 #EB763C/#FFC000/#27A857, 어두운 라운드 배경)를 15개 `bidcast*.html` 모두 `<meta charset>` 다음에 `<link rel="icon" type="image/svg+xml" href="/favicon.svg"/>`로 링크(브라우저의 `/favicon.ico` 404 제거). 새 페이지에도 같은 링크를 넣을 것.
