# 000_PROJECT_MANIFESTO.md

> **신호등옥션 (구 낙찰예보/BidCast)** — 온비드 공매 낙찰가 예측 서비스의 헌장.
>
> 이 문서는 코드보다 오래 산다. 기능은 바뀌어도 여기 적힌 원칙은 바뀌지 않는다.
> 원칙을 바꿔야 한다면 그것은 버그 수정이 아니라 **헌장 개정**이며, 반드시 사람(창업자)이
> 결정하고 이 문서와 chronicle에 기록한다.
>
> 작성: 2026-07-25 · 근거: 실제 코드·봉인 장부·백테스트 실측치 (추정·희망 수치 없음)
>
> **2026-07-26 개정**: 장기 비전으로 **AIOS(Auction Intelligence Operating System)** 채택
> (BC-000 실사 문서, 창업자 승인). 미션을 "예측"에서 "의사결정 지원"으로 재정의(3절),
> 4단계 로드맵으로 단계화(4절). BC-000 정식 문서(작성 중, 외부 실사)가 완성되면
> 이 헌장과 병합하되, Golden Rules(15절)와 충돌하는 항목은 자동 반영하지 않고
> 창업자 결정에 올린다.

---

## 1. Why BidCast Exists

공매(온비드) 입찰은 정보 비대칭의 시장이다. 감정가·최저가·유찰 이력은 공개되지만,
"그래서 얼마에 낙찰될 것인가"라는 단 하나의 질문에는 아무도 검증 가능한 답을 주지 않는다.
초심자는 감으로 지르고, 업자는 경험을 독점한다.

신호등옥션은 이 질문에 **검증 가능한 구간**으로 답하고, 그 과정을 통째로 공개해서
**사용자가 스스로 공매 전문가로 성장하는 도구**가 되기 위해 존재한다.
이것이 창업 취지이며, 모든 기능은 이 한 문장으로 소급 검증한다.

## 2. Industry Problems

1. **검증 불가능한 정확도 주장** — "적중률 99%"류의 점 예측 광고가 만연하지만,
   예측을 사전에 기록하고 사후에 채점한 증거를 공개하는 곳은 없다.
2. **사후 편향(hindsight bias)** — 개찰 후에 "우리는 알고 있었다"고 말하는 것은 예측이 아니다.
3. **블랙박스 AI** — 근거 없는 "AI 분석"이 신뢰를 소모한다. 데이터에 없는 사실을
   그럴듯하게 생성하는 것은 서비스가 아니라 위험이다.
4. **초심자 배제** — 권리분석·매각조건·회차 구조를 모르는 사용자는 학습 경로 없이
   실전(자기 돈)으로 배운다.

## 3. Our Mission

**Prediction은 수단이다. Decision이 목적이다.** (BC-000, 2026-07-26 채택)

우리의 목적은 낙찰가를 맞추는 것이 아니라, 사용자가 데이터·AI·통계·설명 가능한
예측으로 **더 나은 투자 의사결정**을 내리도록 지원하는 것이다.

그 수단이 신뢰받으려면 검증 가능해야 하므로, 우리의 실행 방법은:
**개찰 전에 봉인하고, 개찰 후에 채점하며, 그 전 과정을 공개한다.**
사용자에게는 같은 루프(내 예측 봉인 → 개찰 → 자기 채점)를 제공해
서비스와 함께 성장하게 한다.

## 4. Our Vision

**오늘의 우리 = 정직한 예측 장부. 내일의 우리 = AIOS.**

장기 비전은 **AIOS(Auction Intelligence Operating System)** — 법원경매·온비드·
신탁·민간·NPL 등 서로 다른 표준으로 흩어진 대한민국 경매·공매 데이터를 하나의
신뢰 가능한 지식 체계로 연결하는 표준 플랫폼이다(BC-000). 정보는 존재하지만
지식은 존재하지 않는 시장 — 그 간극이 우리가 메울 자리다.

단, 비전은 단계로만 실행한다. **각 단계는 앞 단계의 실측 성공을 전제로만 착수**:

1. **1단계(현재)**: 온비드 예측 장부로 신뢰 루프 증명 — 봉인·채점·공개가 실제로
   사용자 신뢰를 만드는지. 지금 프로젝트 전체가 AIOS의 첫 관문이다.
2. **2단계(진행 중)**: 온비드 전 자산군 커버 + 근거 레이어(에이전트 8종·외부 데이터) 완성.
3. **3단계**: 두 번째 데이터 소스 1개(후보: 법원경매)를 **표준 스키마**로 편입 —
   "표준 플랫폼"의 실질적 시작이자 ADR-000 재검토 시점. 소스별 데이터 확보 경로
   (공공 API 부재·상용·법적 제약)를 검증한 뒤에만 착수.
4. **4단계**: AIOS — 다중 소스 표준화·비교·분석이 한 체계 위에서 동작.

이와 함께: 사용자가 이드몬에서 아폴론까지(예언자 6등급) 성장하는 동안 엔진도
챔피언/챌린저 루프로 함께 성장하는 **쌍둥이 성장 구조**, 예측 → 근거 → 시뮬레이션
→ 실전 준비까지 하나의 학습 여정.

## 5. Architectural North Star

**"단순한 것이 살아남는다."**

- 정적 HTML(페이지당 1파일, CSS/JS 인라인) + Netlify Functions(서버리스 프록시) + Netlify Blobs(장부).
- 빌드 없음, 프레임워크 없음, 번들러 없음. 15개 페이지 어떤 것도 단독으로 열어 동작한다.
- 모든 비밀키는 서버(환경변수)에만 존재한다. 클라이언트는 프록시만 호출한다.
- **모듈로 확장한다(2026-07-26, AIOS 대비)**: 1인 개발을 전제로, 새 능력은 기존 것을
  뜯어고치지 않고 **레지스트리에 등록**하는 방식으로 붙인다. 이미 그렇게 되어 있는 곳 —
  온비드 27종(`onbid-svc` 레지스트리), 전문 에이전트 8종(`agents.js` 로스터),
  RTMS 10종(`rtms-svc`), 예측 모델(챔피언/챌린저 병행 봉인). 새 데이터 소스·에이전트·
  모델·섹션을 추가할 때 이 패턴을 따르지 않는 설계는 반려한다.
- 상세 결정 기록은 [ADR-000](#adr-000) 참조.

## 6. Core Philosophy

**정직이 곧 제품이다.**

- 봉인된 예측은 **절대 덮어쓰지 않는다** — 불변성이 신뢰 스토리의 전부다.
- 통계적 근거가 없으면 예측하지 않는다(`noBasis` 스킵). 찍지 않는다.
- "99% 정확" 같은 점 예측 주장을 **의도적으로 하지 않는다**(2026-07-19 합의).
  지표는 구간 적중률(목표 95~98%) + 중앙값 오차(%·원화 병기) + 가격대별 분해.
- 실패도 공개한다: 채점 소실 사고(scoreboard n:0 185일), v0.6 기각 — 전부 기록으로 남긴다.

## 7. Product Philosophy

**두 트랙은 절대 섞지 않는다 (2026-07-20 합의).**

- **① 엔진 공식 성적표**: 조건 내 전수·불변·기준 고정. "우리 모델은 이만큼 정확하다"의 공적 근거.
- **② 사용자 개인 트랙**: 관심 물건만 골라 자기 예측·자기 채점. "나는 이만큼 성장했다"의 사적 기록.
- 비유: ①=기상청 공식 예보 적중률, ②=개인이 스스로 맞춰보는 연습.
  개인 연습은 공식 적중률을 훼손하지 않고, UI·문구에서 항상 분리 표기한다.
- 백테스트(과거 재현·검증용 곡선)도 공식 성적표와 라벨로 분리한다.

## 8. Data Philosophy

1. **마스킹은 타협 불가** — 과거 학습에서 낙찰가는 물리적으로 분리 저장한다
   (`hist/feat/*` 개찰 전 피처 vs `hist/win/*` 결과). 예측 모듈은 `hist/win`을
   require조차 하지 않는다. 집계 통계(모집단 낙찰가율 분위수)는 허용하되,
   예측 대상 물건 자신의 낙찰가에 접근하는 경로는 코드 구조로 차단한다.
2. **누수 0의 시험만 인정** — walk-forward(확장 창)로 항상 미래 블록만 예측·채점한다.
3. **실데이터가 아니면 반드시 표시** — 모든 미연동 수치에 "예시 데이터" 칩 부착
   (법적 리스크 메모에서 나온 하드 요구사항).
4. **degrade-gracefully** — API 실패 시 예시 유지·기능 축소. 페이지는 절대 깨지지 않고,
   가짜 초록(허위 정상 표시)도 하지 않는다.

## 9. AI Philosophy

- **에이전트의 정성 분석은 봉인 예측을 직접 수정하지 않는다.** 반영 경로는 두 가지뿐:
  (a) 사용자에게 근거로 표시, (b) 수치 피처화 → 챌린저 → 봉인·채점 검증 통과 후 반영.
- 팩트가 없으면 생성하지 않는다. 전 에이전트 시스템 프롬프트가
  "데이터에 없는 사실 생성 금지·구간으로만·보장 표현 금지"를 강제한다.
- 역할별 면책 자동 부착 — 특히 권리분석은 "법률 자문이 아닙니다"를 첫 문장으로 강제
  (변호사법 리스크로 명칭도 "권리분석 도우미").
- Anthropic API는 서버 프록시(`claude.js`)로만 호출한다: 모델 allowlist(Haiku),
  max_tokens 클램프, 물건·조건당 1회 생성 캐시(비용 통제).
- 공개 자동 보고(주간 감사역)는 LLM을 쓰지 않는다 — 규칙 기반만. 공개 로그에
  헛소리가 섞일 가능성을 원천 차단한다.

## 10. Engineering Philosophy

- **조용한 죽음을 허용하지 않는다** — 모든 예약 함수는 성공·실패 모두 하트비트
  (`_run/*`)를 남기고, 랜딩 자가진단이 오류 또는 30시간 무갱신을 🔴로 표시한다.
  (score-daily가 185일 조용히 죽었던 사고의 재발 방지책.)
- **중간에 죽어도 소실 0** — 커서는 창(window) 단위로 즉시 저장, 채점 마커는
  집계 저장과 같은 최종 배치에 기록. 실패하면 전체 재처리, 성공하면 완결.
- 서버리스 시간 제한(30초)은 청크 병렬(mapLimit) + 시간 예산 + 재개 가능한 상태 저장으로 이긴다.
- 실험은 격리한다: 백테스트는 `bt/*`에만 쓴다. 라이브 장부(pred/predb/agg)는 불가침.
- 검증 없는 "확정" 표기 금지 — 스크린샷 확정 ≠ 작동 확정. 실호출(`?debug=1`)로만 확정한다.

## 11. Design Philosophy

- **야간 보행자 신호등 팔레트**: 밝은 중립 배경, 초록(`--green #00A86B`)=진행/주요 액션,
  빨강(`--red #E5484D`)=경고 전용 — 항상 아이콘/라벨과 병기, 색만으로 의미 전달 금지(접근성).
- 신호등 3단계(STOP/READY/GO)가 서비스 전체의 은유: 랜딩 온보딩, 예언자 6등급(3×2),
  시뮬레이터 가이드(초/중/고급)까지 일관 적용.
- 디자인 시스템 v2: Nunito(제목)/Poppins+Noto Sans KR(본문), 가로형 3도트 신호등 로고.
- UI 카피는 한국어. 과장 없이, 실측은 "🟢 실측", 예시는 "예시 데이터"로 정직하게.

## 12. User Trust

신뢰는 주장하는 것이 아니라 **검증 가능하게 만드는 것**이다.

- 살아있는 성적표(랜딩) + 적중률 랩: 실측만 표시, 표본 0이면 "누적 중"이라고 말한다.
- 엔진 자가진단 섹션: 봉인·채점·학습·연동 상태를 사용자가 직접 확인.
  함수가 죽었으면 "확인 불가"라고 정직하게 표시한다(가짜 초록 금지).
- 오해 소지는 선제 안내: 최저가>감정가(신탁 매각) 배지, "규칙 기반·투자 자문 아님" 라벨,
  기관 건수의 "표본 기준" 명시 등.
- 개인정보·비밀키를 코드/리포에 절대 넣지 않는다. 상태 API도 boolean만 노출.

## 13. Long-Term Strategy

1. **챔피언/챌린저 루프** — 같은 물건에 병행 봉인 → 동일 채점 → 고정 체크포인트로만 승격 판정
   (수시 판정 금지 — 우연에 속지 않기 위해). 승격 결정은 사람이 하고 chronicle에 기록.
2. **과거 학습으로 초기 부트스트랩** — 2026년 1~6월 전수(112,649건)를 마스킹 수집,
   학습량이 늘수록 좋아짐을 백테스트로 입증(8절·14절 실측치 참조).
3. **근거 레이어 확장** — 전문 에이전트 8종(부동산/지역/경쟁/권리/매각조건/뉴스/거시/감사역)
   + 실거래가(RTMS)·부동산원 지수(R-ONE)·한국은행 금리(ECOS)로 예측의 "왜"를 보강.
4. **시세 축(보류 중)** — 실거래 기반 시세 추정 → 시나리오 밴드 → 수익 시뮬레이션.
5. **자산군 커버리지** — 부동산·동산·차량(현재) → 유가증권·정부재산 등(승인 완료, 낮은 우선순위).

## 14. Success Metrics

**목표(고정):**

| 지표 | 목표 | 왜 이 지표인가 |
|---|---|---|
| 구간 적중률 | 95~98% | 주력 지표. 100%가 목표가 아님 — 100%는 구간이 무의미하게 넓다는 뜻 |
| 중앙값 오차 | 지속 하락 | %와 원화 병기 — 사용자 체감 단위 |
| 평균 구간폭(avgWidthPct) | 적중률 유지하며 축소 | "넓게 질러서 맞히기" 방지 감시 지표 |
| 채점 표본 n | 지속 증가 | 모든 주장의 분모 |

**측정 단위는 자산군별이다(2026-07-29 개정).** 위 목표는 유지하되, **적중률·오차·구간폭은
자산군(부동산/자동차/동산)별로 측정·표시**하고 전체 합산 수치는 "합산"임을 반드시 밝힌다.

이유: 자산군마다 예측 난이도가 근본적으로 다르다. 부동산은 감정가·등기부·실거래 같은
표준화된 비교축이 많아 잘 맞고(sim 실측 오차 1~5%), 동산은 "중고 오븐 1대"와 "폐기물
2,376점"이 같은 분류에 들어갈 만큼 물건이 제각각이라 어렵다. 표본이 한 자산군에 쏠리면
합산 숫자는 **정직한데도 다른 자산군 사용자를 오도한다** — 실제로 채점 107건 중 부동산이
1건뿐이던 시점의 전체 48.6%는 사실상 동산 성적이었고, 부동산 사용자에게는 틀린 인상을 줬다.

**자산군별로 나눠 보여주는 것이 더 정직하면서 동시에 더 쓸모있다.** 정직성과 유용성은
여기서 충돌하지 않는다 — 이 둘이 충돌하는 것처럼 보이면 대개 표시 단위를 잘못 잡은 것이다.

**실측 기록(부트스트랩 시점, 2026-07-23~25):**

- 과거 학습 백필: 1~6월 전수 **112,649건** 수집 완료(낙찰가 마스킹 분리 저장).
- walk-forward 백테스트(v0.5): 적중률 **53.5% → 56.4% → 62.8%**,
  중앙값 오차 **56.5% → 43.9% → 41.0%** — 학습이 늘수록 좋아짐을 실증.
- v0.6 챌린저 **기각**: 적중률 상승이 밴드 1.7배 확대의 결과였고 오차는 악화 —
  15절 Golden Rule "넓혀서 맞히는 것은 실력이 아니다"의 첫 적용 사례.
- v0.7 챌린저 **기각**(2026-07-26): 저가율 조건 셀(L4)이 실데이터에선 회차·가격대와 이미
  상관돼 표본만 얇게 쪼개는 과분할 → 3단계 모두 적중률 0.6~1.3%p 하락. 폭을 넓히지 않고도
  기각된 두 번째 사례 — 백테스트 게이트가 열화를 라이브 전에 잡아낸다는 증거.
- 라이브 채점 재가동: n=43 누적 중, 첫 자동 보정 발동(기계장비 w 0.18→0.19),
  약한 세그먼트 식별(기계장비 18% vs 차량 85%).
- 외부 연동 6종 전부 활성: onbid·anthropic·naverNews·kakaoMap·ecos·rone.

**모델 승격 체크포인트(2026-07-20 고정 — 변경 금지):**
① 조기 승격: 비교 100건 시점 승률 62%+ ② 표준 승격: 300건 시점 55%+ &&
세그먼트 붕괴 없음(어떤 용도·가격대도 10%p 이상 하락 금지) && 동률이면 폭이 좁을 것
③ 조기 탈락: 200건 시점 50% 이하.

## 15. Golden Rules

깨면 안 되는 규칙. 우선순위 충돌 시 위가 이긴다.

1. **봉인은 불변이다.** 봉인된 예측을 수정·삭제·재작성하는 코드는 존재해선 안 된다.
2. **예측 모듈은 예측 대상의 낙찰가에 접근할 수 없다.** 마스킹은 규율이 아니라 구조다.
3. **두 트랙(공식 성적표 / 개인 학습)은 UI에서 절대 섞지 않는다.**
4. **넓혀서 맞히는 것은 실력이 아니다.** 적중률은 항상 구간폭과 함께 판정한다.
5. **판정 기준은 미리 고정하고, 수시로 바꾸지 않는다.** (승격 체크포인트·등급 기준·성적표 기준)
6. **근거 없는 수치는 표시하지 않는다.** 실측 아니면 "예시 데이터", 확인 불가면 "확인 불가".
7. **모델 채택·변경의 최종 결정은 사람이 한다.** 자동화는 관찰·제안까지만.
8. **비밀키는 서버 환경변수에만.** 코드·리포·클라이언트에 절대 넣지 않는다.
9. **페이지는 절대 깨지지 않는다.** 모든 외부 의존은 degrade-gracefully.
10. **실패를 기록한다.** 사고·기각·소실은 숨기지 않고 chronicle과 문서에 남긴다.
11. **시점 기준을 어기지 않는다 (미래를 훔쳐보지 않는다).** 어떤 시점 T를 기준으로 예측할 때, **T 이후에 발생·공개된 어떤 데이터도 예측 입력에 쓰지 않는다** — 개찰 결과·낙찰가뿐 아니라 T 이후의 캠코 통계·부동산원 지수·금리·뉴스·시세 등 **모든 미래 정보**를 포함한다. 학습·백테스트·모의 라이브 모두 **T 이전 데이터만으로 예측**하고, T 이후 값은 **오직 채점(정답 확인)에만** 쓴다. 강제 수단: 낙찰가 마스킹(feat/win 물리 분리) + walk-forward(과거 학습→미래 예측) + purge/embargo(같은 공고가 학습·시험에 겹치지 않게) + **as-of 외부데이터**(통계·지수·금리는 예측 시점 기준값). 위반은 성적의 의미를 파괴하므로 절대 금지.

## 16. Final Declaration

우리는 미래를 아는 척하지 않는다.
우리는 **개찰 전에 쓰고, 개찰 후에 채점받고, 그 장부를 공개하는 서비스**다.
사용자가 우리를 믿는 이유는 우리가 정확하다고 말해서가 아니라,
**우리가 틀린 기록조차 지우지 않기 때문**이어야 한다.

그리고 이 서비스의 최종 목적은 예측기가 아니라 사람이다 —
사용자가 스스로 공매 전문가로 성장했을 때, 이 프로젝트는 성공한 것이다.

---

## Appendix A — 시스템 인벤토리 (2026-07-25 기준)

**정적 페이지 15종** (`bidcast*.html`, 전부 단일 파일 자기완결):
랜딩 / 물건검색(리스트) / 상세 / 마이(개인 허브) / 적중률 랩 / 캘린더 / 비교함 / 지도 /
예보봇 / 시뮬레이터 / 인사이트 / 카테고리(SEO) / 파트너 / 요금제 / 고객지원.

**서버리스 함수** (`netlify/functions/`):

| 계열 | 함수 | 역할 |
|---|---|---|
| 예측 루프 | predict-daily | 매일 07:00 KST 봉인(챔피언+챌린저 병행) |
| | score-daily | 매일 19:30 KST 자동 채점 + 폭 보정(calib) |
| | collect-history | 매일 20:10 KST 증분 학습 수집 |
| | collect-backfill | 1~6월 전수 마스킹 수집(완료, 수동 재실행용) |
| | backtest | walk-forward 3단계 + 모델 비교(bt/* 격리) |
| | hist-stats | 다차원 셀 통계 조회(공용 데이터 레이어) |
| | audit-weekly | 일요일 주간 감사(규칙 기반, 에이전트 ⑧) |
| 온비드 프록시 | onbid-search / -mvast-search / -vhcl-search | 부동산·동산·차량 목록 |
| | onbid-detail / -mvast-detail | 상세 |
| | onbid-bidresults / -bidinfo / -calendar / -svc(레지스트리 27종) | 개찰 결과·캘린더·범용 |
| 외부 데이터 | rtms-svc(실거래 10종) / rone-svc(부동산원) / ecos-svc(한은) / naver-news | 근거 레이어 |
| AI | claude(프록시) / agents(전문 에이전트 8종 라우터) | 생성·분석 |
| 운영 | scoreboard / integrations-status / map-config | 공개 성적표·자가진단·지도 키 |

## Appendix B — 데이터 스토어 스키마 (Netlify Blobs `ledger`)

| 네임스페이스 | 내용 | 불변성 |
|---|---|---|
| `pred/{id}_{cdtn}` | 챔피언 봉인 예측 [lo,mid,hi] | **불변(덮어쓰기 금지)** |
| `predb/{id}_{cdtn}` | 챌린저 봉인 예측(v0.5 분위수) | **불변** |
| `scored/*` | 채점 마커(이중 채점 방지) | 최종 배치에서만 기록 |
| `agg` / `aggB` / `calib` / `log` | 집계·보정·학습 로그 | 갱신형 |
| `hist/feat/*` | 개찰 전 피처(낙찰가 없음) | 마스킹 분리의 한쪽 |
| `hist/win/*` | 개찰 결과(낙찰가) — 예측 모듈 접근 금지 | 마스킹 분리의 다른쪽 |
| `hist/_cells` | 다차원 분위수 셀 캐시 | 재빌드형 |
| `bt/*` | 백테스트(라이브 무관 격리) | 버전별 재실행 |
| `agent/{agent}/{id}_{cdtn}` | 에이전트 분석 캐시(1회 생성) | 캐시 |
| `curated/latest` | "오늘의 주목 물건" 큐레이션(기회 점수·근거 태그) | 일 1회 갱신 |
| `_run/*` | 함수 하트비트 | 갱신형 |
| `audit/latest` / chronicle | 감사·연혁 | append-only |

클라이언트 localStorage(개인 트랙): `bidcast:likes/likesnap/recent/alerts/mypred/nick/sims/battle`.

## Appendix C — 고정 결정 대장 (변경 시 헌장 개정 필요)

| 날짜 | 결정 | 근거 |
|---|---|---|
| 2026-07-19 | 점 예측 정확도 주장 금지, 구간 지표 체계 채택 | 정직성 원칙 합의 |
| 2026-07-19 | 리브랜딩: 낙찰예보 → 신호등옥션 | — |
| 2026-07-20 | 두 트랙(공식/개인) 절대 분리 | 성적표 의미 보전 |
| 2026-07-20 | 승격 체크포인트 3단계 고정, 수시 판정 금지 | 우연에 속지 않기 |
| 2026-07-20 | 에이전트는 봉인 예측 직접 수정 금지(표시/피처화 경로만) | AI 철학 |
| 2026-07-22 | 낙찰가 무조건 마스킹 — 접근 경로 자체 차단 | 사용자 지시(타협 불가) |
| 2026-07-23 | 과거 학습 스코프 = 2026년 1~6월 전수 | 서비스 시작 직후라 90일 라이브 불가 |
| 2026-07-23 | 채점 마커는 집계와 같은 최종 배치에 기록 | 185일 소실 사고 재발 방지 |
| 2026-07-23 | v0.6 기각 — 밴드 확대로 얻은 적중률 불인정 | Golden Rule 4 첫 적용 |
| 2026-07-26 | v0.7 기각 — 저가율 셀이 실데이터에선 과분할(표본 희석)로 열화 | 백테스트 실측(승격 기준 미달), v0.5 유지 |
| 2026-07-26 | AIOS 장기 비전 채택 — 단, 4단계 로드맵으로만 실행(현재=1단계 관문) | BC-000 실사, 창업자 승인 |
| 2026-07-26 | 미션 재정의: "Prediction은 수단, Decision이 목적" | BC-000, 창업 취지와 합치 |
| 2026-07-26 | BC-000 병합 시 Golden Rules 충돌 항목은 자동 반영 금지, 창업자 결정 | 충돌 플래그 처리 원칙 |
| 2026-07-26 | 확장은 레지스트리 등록 방식(모듈)으로만 — 1인 개발 전제 | AIOS 대비 확장성 원칙 |
| 2026-07-26 | 큐레이션 엔진은 소스 중립 공통 팩트만 읽는다(Appendix D 계약) | AIOS 다중 소스 확장 경계 |
| 2026-07-26 | 큐레이션 점수 = 분석 주목도, 투자 추천 아님(근거 태그로 분해) | 법적 리스크·Golden Rule 6 |
| 2026-07-26 | 회원등록(정식 인증)은 의도적 미구현 — 정식 오픈 직전 착수 | 데모 로그인+localStorage로 1단계 충분, ADR-000 경계 밖 |
| 2026-07-26 | 시세 축 보류 해제 — 단, 신뢰도 프로브(market-est)부터, 품질 미달 시 중단 | "얼마에 팔 수 있나"가 최종 질문(마진 = 시세 − 낙찰가 − 비용의 전제), 창업자 결정 |
| 2026-07-26 | 시세는 "한 숫자" 금지 — ㎡당 범위(p25~p75) + 표본수 + 신뢰도 등급으로만 | Golden Rule 6, 매칭 정밀도의 정직한 표현 |
| 2026-07-29 | **성적 측정·표시 단위는 자산군별.** 목표(95~98%)는 유지하되 적중률·오차·구간폭을 자산군별로 측정하고, 합산 수치는 "합산"임을 명시. 표본 70% 이상 쏠림은 감사역이 경고 | 합산 하나가 표본 구성에 끌려가 다른 자산군 사용자를 오도(부동산 1건일 때 전체 48.6%). 창업자 승인 |
| 2026-07-29 | **승격 판정은 상대전적(headToHead)으로만.** 구간 적중률은 참고 지표. + 폭 가드: 챌린저 평균 폭이 챔피언의 2배 초과면 승격 판정 보류 | 14절 "비교 N건 시점 승률"의 오구현 교정 — 적중률로 판정하면 Golden Rule 4를 감사역이 승인하게 됨 |
| 2026-07-29 | **근거 없으면 예측하지 않는다를 챌린저 전 경로에 적용.** 최저가 0이면 [0,0,0] 대신 noBasis 스킵, 스킵 건수는 coverage로 노출 | 예측이 아닌 값을 빗나감으로 집계해 성적 왜곡. Golden Rule 6 |
| 2026-07-27 | **시점 기준 정직성(No Look-Ahead) — Golden Rule 11 신설.** 기준 시점 T 이후 데이터(낙찰가·통계·지수·금리·뉴스·시세 전부) 예측 입력 사용 금지, as-of만 허용. 오프라인 학습장·모의 라이브·2025 백필 착수 전 못박음 | 창업자 지시, "미래를 훔쳐보면 성적이 무의미" |

**회원등록 착수 트리거**(하나라도 실사용 요구로 확인될 때): ① 결제(요금제 과금 — 가장 강력)
② 실 알림(이메일/푸시) ③ 기기 간 동기화. 그 전까지는 순수 프론트 데모 로그인 유지
(진짜 회원 = 별도 인증 서비스 필요, 후보 Netlify Identity/Supabase Auth). 상세는 CLAUDE.md
"보류 중인 작업" 참조.

## Appendix D — 큐레이션 공통 팩트 계약 (소스 중립 · AIOS 확장 경계)

큐레이션 엔진(`lib/curation.js`)은 **아래 공통 팩트만** 읽는다. 온비드·법원경매·신탁·NPL
어느 소스든 각자의 원시 응답을 이 스키마로 바꾸는 **어댑터 하나**만 작성하면 점수 로직은
한 줄도 바뀌지 않는다(예: `fromOnbid`). 이 경계가 흐려지면 — 엔진이 특정 소스의 전용
필드명을 직접 만지면 — AIOS 확장성이 무너지므로, 새 소스 추가 PR은 이 계약을 지켜야 한다.

```
{ source,            // 'onbid' | 'court' | 'trust' | 'npl' …
  id, cdtn,          // 물건 식별자
  assetClass, type, usage, region,
  apsl, low,         // 감정가 · 최저가 (만원)
  failCount, round,  // 유찰 횟수 · 회차
  bidEnd }           // 'yyyyMMddHHmm' | ''
```

- **점수 = 분석 주목도이지 투자 추천이 아니다.** 마법 숫자로 뭉치지 않고 근거 태그(reasons)로
  분해해 노출한다. 가점 근거가 하나도 없으면 큐레이션에 올리지 않는다(null).
- **소스별로 다른 두 가지**(정직하게 인정): ① 통계 셀은 소스별 네임스페이스로 분리한다
  (법원경매 낙찰가율 ≠ 온비드). ② 데이터 확보 경로가 소스마다 다르다(공공 API·상용·사적) —
  이는 코드가 아니라 소싱 문제이며 로드맵 3단계의 착수 전제(§4).
- **큐레이션도 채점 대상**(향후): 주목 물건이 개찰 후 실제로 어땠는지 기록해 큐레이션 품질
  자체에 성적표를 붙인다 — 봉인·채점 철학의 확장.

## ADR-000 — 아키텍처 기본 결정

**상태**: 채택(프로젝트 개시 시점) · **재검토 조건**: 실사용자 트래픽이 정적+서버리스 한계를 실증할 때

**결정**: 정적 단일 파일 HTML + Netlify Functions + Netlify Blobs. 빌드·프레임워크·DB 없음.

**맥락**: 1인 창업 프로토타입. 검증해야 할 것은 기술 스택이 아니라
"정직한 예측 장부가 신뢰를 만드는가"라는 가설. 운영 부담과 고정비를 0에 가깝게 유지해야 한다.

**결과(감수하는 트레이드오프)**:
- (+) 배포=push, 서버 관리 0, 페이지 단독 동작, 장애 반경 최소.
- (−) 함수 30초 제한 → 청크 병렬·커서 재개·시간 예산 패턴으로 해결(실증됨: 112k 수집).
- (−) 공용 컴포넌트 없음 → 페이지 간 중복(auth modal 등)은 컨벤션으로 통제.
- (−) 무료 플랜 로그 제한 → `?debug=1`·하트비트·자가진단으로 관측성 자체 구축.

**금지**: 이 결정을 우회하는 부분 도입(번들러, 외부 JS 파일, 프레임워크 컴포넌트)은
전면 개정 없이 허용하지 않는다.
